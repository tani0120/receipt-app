# Phase 5 Supabase実装ノート

## 📋 目的

モック（UIテスト用）からSupabase（本番環境）への移行手順と注意事項を記載

## 🔄 モックと本番の差分

### モック型定義（現在）

[journal_phase5_mock.type.ts](file:///C:/dev/receipt-app/src/mocks/types/journal_phase5_mock.type.ts)

**省略されているフィールド**:
- `client_id`（顧問先ID）
- `status_updated_by`（作業者ID）
- `status_updated_at`（ステータス更新日時）
- `created_at`、`updated_at`（タイムスタンプ）
- `read_at`、`exported_at`、`deleted_at`（各種日時）
- `exported_by`、`deleted_by`（作業者情報）
- `export_exclude`、`export_exclude_reason`（出力制御）← 出力列実装時にモック型にも追加予定（task_current §2参照）

### 本番型定義（Supabase実装時）

**追加が必要なフィールド** → [migration.sql](file:///C:/dev/receipt-app/docs/genzai/02_database_schema/journal/migration.sql)参照

## 📝 実装手順

### ステップ1: DDL実行

```bash
# Supabaseダッシュボード > SQL Editor で実行
psql -h your-project.supabase.co -U postgres -d postgres -f migration.sql
```

または、Supabase CLI使用:
```bash
supabase migration new phase5_journals
# 生成されたファイルに migration.sql の内容をコピー
supabase db push
```

### ステップ2: 型定義の作成

`src/types/journal.ts`を新規作成:

```typescript
export interface Journal {
  // 基本情報
  id: string;
  client_id: string;                    // ← モックにはない
  receipt_id: string | null;
  display_order: number;
  voucher_date: string;                  // ISO 8601: YYYY-MM-DD（伝票日。証憑記載の日付=fact）
  description: string;

  // ステータス管理
  status: JournalStatus;
  status_updated_at: string;             // ← モックにはない
  status_updated_by: string;             // ← モックにはない

  // タイムスタンプ
  created_at: string;                    // ← モックにはない
  updated_at: string;                    // ← モックにはない

  // 未読/既読
  is_read: boolean;
  read_at: string | null;                // ← モックにはない

  // N対N複合仕訳（別テーブル）
  debit_entries: JournalEntry[];
  credit_entries: JournalEntry[];

  // ラベル
  labels: JournalLabel[];

  // クレジットカード払い判定（Gemini層A、独立カラム）
  is_credit_card_payment: boolean;        // クレカ払い: 💳アイコン表示

  // ルール関連
  rule_id: string | null;
  rule_confidence: number | null;

  // インボイス関連
  invoice_status: 'qualified' | 'not_qualified' | null;
  invoice_number: string | null;

  // メモ関連（証票メモ）
  memo: string | null;
  memo_author: string | null;
  memo_target: string | null;
  memo_created_at: string | null;

  // スタッフノート（モック実装済み: staff_notes管理。要対応4種のデータ）
  // ⚠️ モックではstaff_notes: StaffNotes | null で管理
  // 本番ではstaff_notesテーブル分離 or JSONB化を検討（Phase C）
  staff_notes: StaffNotes | null;        // ← モック実装済み（2026-02-21）

  // 出力管理
  exported_at: string | null;            // ← モックにはない
  exported_by: string | null;            // ← モックにはない
  export_exclude: boolean;               // ← モックにはない（出力列実装時に追加予定）
  export_exclude_reason: string | null;  // ← モックにはない（出力列実装時に追加予定）

  // ゴミ箱
  deleted_at: string | null;             // ← モックにはない
  deleted_by: string | null;             // ← モックにはない
}
```

```typescript
import type { Yen } from '@/shared/types/yen'

export interface JournalEntry {
  id: string;
  journal_id: string;
  entry_type: 'debit' | 'credit';
  line_number: number;
  account: string;
  sub_account: string | null;
  amount: Yen;
  tax_category: string | null;
  created_at: string;
}
```

### ステップ3: Supabaseクライアントの設定

```typescript
// src/lib/supabase.ts
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = import.meta.env.VITE_SUPABASE_URL;
const supabaseKey = import.meta.env.VITE_SUPABASE_ANON_KEY;

export const supabase = createClient(supabaseUrl, supabaseKey);
```

### ステップ4: API層の実装

```typescript
// src/api/journals.ts
import { supabase } from '@/lib/supabase';
import type { Journal } from '@/types/journal';

export async function getJournals(clientId: string): Promise<Journal[]> {
  const { data, error } = await supabase
    .from('journals')
    .select(`
      *,
      debit_entries:journal_entries!journal_id(entry_type, line_number, account, sub_account, amount, tax_category),
      credit_entries:journal_entries!journal_id(entry_type, line_number, account, sub_account, amount, tax_category)
    `)
    .eq('client_id', clientId)
    .is('deleted_at', null)
    .order('display_order', { ascending: true });

  if (error) throw error;
  return data as Journal[];
}

export async function updateJournal(
  journalId: string,
  updates: Partial<Journal>,
  userId: string
): Promise<Journal> {
  // ガード句: exportedは編集不可
  const { data: current } = await supabase
    .from('journals')
    .select('status')
    .eq('id', journalId)
    .single();

  if (current?.status === 'exported') {
    throw new Error('CSV出力済みの仕訳は編集できません');
  }

  const { data, error } = await supabase
    .from('journals')
    .update({
      ...updates,
      status_updated_by: userId,
      status_updated_at: new Date().toISOString()
    })
    .eq('id', journalId)
    .select()
    .single();

  if (error) throw error;
  return data as Journal;
}
```

### ステップ5: テストデータの投入

```typescript
// scripts/seed-journals.ts
import { supabase } from '@/lib/supabase';
import { mockJournalsPhase5 } from '@/mocks/data/journal_test_fixture_30cases';

async function seedJournals() {
  const clientId = 'your-client-id';
  const userId = 'your-user-id';

  for (const mock of mockJournalsPhase5) {
    // journals テーブルに挿入
    const { data: journal, error: journalError } = await supabase
      .from('journals')
      .insert({
        client_id: clientId,
        display_order: mock.display_order,
        voucher_date: mock.voucher_date,
        description: mock.description,
        receipt_id: mock.receipt_id,
        status: mock.status,
        status_updated_by: userId,
        is_read: mock.is_read,
        labels: mock.labels,
        rule_id: mock.rule_id,
        rule_confidence: mock.rule_confidence,
        invoice_status: mock.invoice_status,
        invoice_number: mock.invoice_number,
        memo: mock.memo,
        memo_author: mock.memo_author,
        memo_target: mock.memo_target,
        memo_created_at: mock.memo_created_at
      })
      .select()
      .single();

    if (journalError) throw journalError;

    // journal_entries テーブルに挿入（借方）
    for (let i = 0; i < mock.debit_entries.length; i++) {
      const entry = mock.debit_entries[i];
      await supabase.from('journal_entries').insert({
        journal_id: journal.id,
        entry_type: 'debit',
        line_number: i + 1,
        account: entry.account,
        sub_account: entry.sub_account,
        amount: entry.amount,
        tax_category: entry.tax_category
      });
    }

    // journal_entries テーブルに挿入（貸方）
    for (let i = 0; i < mock.credit_entries.length; i++) {
      const entry = mock.credit_entries[i];
      await supabase.from('journal_entries').insert({
        journal_id: journal.id,
        entry_type: 'credit',
        line_number: i + 1,
        account: entry.account,
        sub_account: entry.sub_account,
        amount: entry.amount,
        tax_category: entry.tax_category
      });
    }
  }

  console.log('✅ テストデータの投入が完了しました');
}

seedJournals();
```

## ⚠️ 重要な注意事項

### 1. exportedステータスのガード

**必須**: API層で以下のガード句を実装

```typescript
if (journal.status === 'exported') {
  throw new BusinessRuleError(
    'CSV出力済みの仕訳は編集できません。' +
    'MF側で修正するか、管理者に問い合わせてください。',
    'EXPORTED_JOURNAL_READONLY'
  );
}
```

**モックUI側のexported制限（2026-02-21実装済み）**:

| 操作種別 | 制限内容 |
|---|---|
| 個別DD: 未読/既読トグル | disabled + グレーアウト（ゴミ箱と同じ見た目） |
| 個別DD: 対象/対象外トグル | disabled + グレーアウト（ゴミ箱と同じ見た目） |
| 個別DD: コピー | **許可**（コピー先は常にstatus=null）メッセージ「○○を未出力にコピーしますか？」 |
| 個別DD: ゴミ箱 | disabled + グレーアウト（既存の実装） |
| 一括: 未読/既読 | exported行スキップ。全件exportedなら「実行不可」メッセージ表示 |
| 一括: 対象/対象外 | exported行スキップ。全件exportedなら「実行不可」メッセージ表示 |
| 一括: コピー | **全件許可**。メッセージ「N件を未出力にコピーしますか？」 |

### 2. RLS（Row Level Security）

**必須**: 顧問先IDによるアクセス制限を設定

```sql
CREATE POLICY journals_client_isolation ON journals
  FOR ALL
  USING (client_id = auth.uid()::uuid OR auth.jwt() ->> 'role' = 'admin');
```

### 3. トランザクション処理

CSV出力時は必ずトランザクションで処理:

```typescript
async function exportToCSV(clientId: string, userId: string) {
  const { data, error } = await supabase.rpc('export_journals', {
    p_client_id: clientId,
    p_user_id: userId
  });

  if (error) throw error;
  return data;
}
```

対応するSQL Function:
```sql
CREATE OR REPLACE FUNCTION export_journals(
  p_client_id UUID,
  p_user_id UUID
) RETURNS TABLE(filename TEXT, count INTEGER) AS $$
DECLARE
  v_batch_id UUID;
  v_filename TEXT;
  v_count INTEGER;
BEGIN
  -- 1. バッチ作成
  INSERT INTO export_batches (client_id, exported_by)
  VALUES (p_client_id, p_user_id)
  RETURNING id, filename INTO v_batch_id, v_filename;

  -- 2. 仕訳をexportedに更新
  UPDATE journals
  SET status = 'exported',
      exported_at = NOW(),
      exported_by = p_user_id
  WHERE client_id = p_client_id
    AND status IS NULL  -- 未出力のみ対象
    AND export_exclude = FALSE
  RETURNING id INTO v_count;

  RETURN QUERY SELECT v_filename, v_count;
END;
$$ LANGUAGE plpgsql;
```

## 🧪 動作確認

### 1. DDL確認

```sql
\d journals
\d journal_entries
\d export_batches
\d journal_exports
```

### 2. インデックス確認

```sql
\di journals*
```

### 3. CHECK制約確認

```sql
SELECT conname, pg_get_constraintdef(oid)
FROM pg_constraint
WHERE conrelid = 'journals'::regclass
  AND contype = 'c';
```

### 4. RLS確認

```sql
SELECT schemaname, tablename, policyname, permissive, roles, cmd, qual
FROM pg_policies
WHERE tablename = 'journals';
```

## 📦 ファイル配置

```
docs/genzai/02_database_schema/journal/
├── journal_v2_20260214.md    # 設計書（既存）
├── migration.sql              # DDL（本ファイル）
├── 実装ノート.md              # 本ファイル
└── API設計書.md               # 次に作成

src/
├── types/
│   └── journal.ts             # 本番型定義（新規作成）
├── api/
│   └── journals.ts            # API層（新規作成）
└── lib/
    └── supabase.ts            # Supabaseクライアント（新規作成）
```

## ✅ チェックリスト

- [ ] migration.sql実行
- [ ] 型定義ファイル作成（journal.ts）
- [ ] Supabaseクライアント設定
- [ ] API層実装（journals.ts）
- [ ] ガード句実装（exportedチェック）
- [ ] RLS設定
- [ ] テストデータ投入
- [ ] 動作確認（DDL、インデックス、CHECK制約、RLS）
- [ ] モックからの切り替えテスト

---

## 🎨 要対応列UI実装（2026-02-21更新: staff_notesベース）

### 1. 要対応列 — staff_notes管理（4種）

> **⚠️ 2026-02-21変更**: 要対応ステータスは`labels`配列からjournal.`staff_notes`オブジェクトに移行。
> 型定義: `src/mocks/types/staff_notes.ts`（StaffNoteKey, StaffNote, StaffNotes）

#### 表示仕様

| FAアイコン | キー | label | 意味 | 色（非選択時）| 色（選択時）|
|-----------|------|-------|------|------------|-----------|
| `fa-file-circle-exclamation` | `NEED_DOCUMENT` | 書類が不足 | 資料が必要 | `text-gray-400` | `text-red-600` |
| `fa-circle-question` | `NEED_INFO` | 情報が不足 | 確認が必要 | `text-gray-400` | `text-amber-600` |
| `fa-thumbtack` | `REMINDER` | 備忘メモ | 自分用備忘 | `text-gray-400` | `text-blue-600` |
| `fa-comments` | `NEED_CONSULT` | 社内相談する | 相談が必要 | `text-gray-400` | `text-purple-600` |

#### HTML構造（実装済み）

```vue
<!-- JournalListLevel3Mock.vue -->
<template>
  <!-- 要対応: 4FAアイコン（staffNoteConfig駆動） -->
  <div class="flex gap-0.5">
    <button v-for="noteKey in staffNoteKeys" :key="noteKey"
            @click="toggleStaffNote(journal.id, noteKey)"
            @mouseenter="showNeedPopup(journal.id, noteKey)"
            @mouseleave="scheduleHideNeedPopup()"
            :class="getStaffNoteEnabled(journal, noteKey)
              ? staffNoteConfig[noteKey].activeColor
              : 'text-gray-400'"
            class="text-[10px] hover:scale-110 transition-transform"
            :title="staffNoteConfig[noteKey].label">
      <i :class="['fa-solid', staffNoteConfig[noteKey].icon]"></i>
    </button>
  </div>
</template>
```

---

### 2. toggleStaffNote関数の実装（2026-02-21更新）

```typescript
// JournalListLevel3Mock.vue <script setup>
import { createEmptyStaffNotes, STAFF_NOTE_KEYS } from '../types/staff_notes';
import type { StaffNoteKey } from '../types/staff_notes';

function toggleStaffNote(journalId: string, key: StaffNoteKey) {
  const journal = localJournals.value.find(j => j.id === journalId);
  if (!journal) return;
  if (!journal.staff_notes) {
    journal.staff_notes = createEmptyStaffNotes();
  }
  journal.staff_notes[key].enabled = !journal.staff_notes[key].enabled;
}
```

---

### 3. API実装（本番環境）

> **⚠️ Phase C検討事項**: モックでは`staff_notes`をJSONオブジェクトとしてjournal内に保持。
> 本番では`staff_notes`テーブル分離（journal_id + key + enabled + text + chatwork_url）
> またはJSONBカラムとして保持するか、Phase Cで決定する。

```typescript
// src/api/journals.ts — Phase C実装時のイメージ
// ⚠️ 要対応ステータスはlabelsからstaff_notesに移行（2026-02-21決定）
export async function toggleStaffNote(
  journalId: string,
  key: 'NEED_DOCUMENT' | 'NEED_INFO' | 'REMINDER' | 'NEED_CONSULT',
  userId: string
): Promise<Journal> {
  // ガード句: 出力済みチェック
  const { data: current } = await supabase
    .from('journals')
    .select('status, staff_notes')
    .eq('id', journalId)
    .single();

  if (!current) throw new NotFoundError('仕訳が見つかりません');
  if (current.status === 'exported') {
    throw new BusinessRuleError('CSV出力済みの仕訳は編集できません');
  }

  // staff_notesのトグル処理
  const notes = current.staff_notes || createEmptyStaffNotes();
  notes[key].enabled = !notes[key].enabled;

  const { data, error } = await supabase
    .from('journals')
    .update({
      staff_notes: notes,
      updated_at: new Date().toISOString()
    })
    .eq('id', journalId)
    .select()
    .single();

  if (error) throw error;
  return data as Journal;
}
```

---

### 4. テスト項目

- [ ] 各アイコンをクリックして色が変わることを確認
- [ ] ラベルが追加/削除されることを確認
- [ ] フラグ追加時に未読になることを確認
- [ ] 複数のフラグを同時に立てられることを確認
- [ ] 出力済み仕訳は編集不可エラーになることを確認
