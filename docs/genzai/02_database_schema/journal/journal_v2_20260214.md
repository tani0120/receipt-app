# Phase 5 完全スキーマ定義 v2.0（定義B準拠）

**作成日**: 2026-02-14  
**更新**: 定義B（2026-02-13夜）を正式採用。伝票日設計追加（2026-03-03）。UI列幅リサイズ追加（2026-03-19）。列数更新・ID形式統一（2026-03-19）。警告ラベル拡張+MF CSVエクスポート実装（2026-03-27）。useAccountSettingsフォールバック内部化（2026-03-29）  
**目的**: 協力型フロー + Streamed互換の完全なスキーマ定義  
**思想**: 業務効率SaaS、MF拡張レイヤー、Streamed準拠

---

## 🎯 設計思想

### 責任範囲の明確化

| システム | 責任範囲 | 境界線 |
|---------|---------|--------|
| **本システム** | 仕訳作成〜CSV出力まで | `exported` status |
| **マネーフォワード** | CSV取り込み後〜決算確定まで | インポート完了 |

**原則**:
- ✅ MFが会計の真実（Source of Truth）
- ✅ 本システムは業務効率・統制レイヤー
- ❌ 完全同期しない（差分検知のみ）

### 日付設計（2026-03-03確定）

- **伝票日（voucherDate）のみ保持**。取引日・計上日は持たない
- **nullable可**: 証憑から日付が読み取れない場合がある
- **保存時強制バリデーションなし**: 日付は表示するが責任は持たない
- **税区分期間制御はUI側のみ**: 伝票日がある場合のみ、有効期間外の税区分を非表示

**根拠**: STREAMEDも証憑日付ベース。MF/freeeも仕訳日ベース。過剰設計回避。

---

## 📊 1. status定義（1つ、シンプル）

### ENUM型定義

```sql
CREATE TYPE journal_status AS ENUM (
  'exported'   -- 出力済み（CSV出力完了、編集不可）
);
```

### 詳細定義

| status | 日本語 | 意味 | 編集可否 | 責任範囲 |
|--------|--------|------|----------|----------|
| `exported` | 出力済み | CSV出力完了 | ❌ 不可 | MFへ引き渡し完了 |
| `null` | 未出力 | 通常の仕訳（デフォルト）| ✅ 可能 | 本システム |

### 状態遷移図

```
null → exported
```

**特徴**:
- `null`: デフォルト状態、編集可能
- `exported`: 一方通行（出力後は編集不可）

### 設計判断の根拠

**旧ステータス廃止の理由**:
- 旧: `pending`, `help`, `soudan`, `kakunin` → 新: ラベルで表現
- 理由: ステータスは「出力済みかどうか」のみを管理すべき
- Streamed準拠: 出力=完了、ステータスはシンプルに

---

## 🏷️ 2. label定義（22個、非排他的）

### 警告ラベル（10個）

| # | ラベル | 日本語定義 | 色 | 判定ロジック | 採用 | 責任主体 |
|---|--------|-----------|:--:|------------|:---:|---------||
| 1 | `DEBIT_CREDIT_MISMATCH` | 借方貸方の合計額不一致 | 🔴 赤 | 仕訳候補の借方合計額 ≠ 貸方合計額 | ✅ | AI（仕訳推定ミス） |
| 2 | `DATE_UNKNOWN` | 日付が不明 | 🔴 赤 | `voucher_date === null`。`on_document`（項目存在フラグ）でホバーメッセージ分岐 | ✅ | AI/証憑品質 |
| 3 | `ACCOUNT_UNKNOWN` | 勘定科目が不明 | 🔴 赤 | `account === null`。`on_document`（項目存在フラグ）でホバーメッセージ分岐 | ✅ | AI/証憑品質 |
| 4 | `AMOUNT_UNCLEAR` | 内訳が不明瞭な金額あり | 🔴 赤 | `amount === null`。`on_document`（項目存在フラグ）でホバーメッセージ分岐 | ✅ | AI/証憑品質 |
| 5 | `DUPLICATE_CONFIRMED` | 完全重複：同一画像レベル | 🔴 赤 | ファイルハッシュ一致、または同一画像の再取込 | ✅ | 取込処理 |
| 6 | `MULTIPLE_VOUCHERS` | 複数の証票あり | 🔴 赤 | has_multiple_vouchers==true（Geminiが画像から検出） | ✅ | AI（画像解析） |
| 7 | `DUPLICATE_SUSPECT` | 重複疑い：日付等一致、定期支払等の可能性有 | 🟡 黄 | 同バッチ内or過去仕訳と日付+金額+証票タイプが一致 | ✅ | 担当者が判断 |
| 8 | `DATE_OUT_OF_RANGE` | 会計期間外または未来日付 | 🟡 黄 | 伝票日が会計期間外 or 未来日付 | ✅ | AI or 顧問先 |
| 9 | `UNREADABLE_ESTIMATED` | 判読困難（AI推測値） | 🟡 黄 | 項目は存在するが読みにくく、AIが推測で値を入れた | ✅ | 担当者が検証 |
| 10 | `MEMO_DETECTED` | 証票に手書きメモあり | 🟡 黄 | handwritten_flag=='MEANINGFUL'（会計的に意味のある手書き。角印・受領印・スタンプは除外） | ✅ | AI（画像解析） |

#### モック層追加警告（6種 — 2026-03-22〜03-29実装。JournalLabelMock型で定義）

> **⚠️ domain層のJournalLabelには含まない**。モック検証用のバリデーション警告。Phase C以降でuseJournalValidation composable化予定。
> `syncWarningLabelsCore()`（[journalWarningSync.ts](file:///c:/dev/receipt-app/src/mocks/utils/journalWarningSync.ts)）で計算。

| # | ラベル | 日本語定義 | 色 | 判定ロジック | 採用 | 責任主体 |
|---|--------|-----------|:--:|------------|:---:|--------|
| 11 | `CATEGORY_CONFLICT` | 貸借科目矛盾（5分類バリデーション） | 🔴 赤 | 全借方×全貸方のクロスチェック。accountGroup→5分類（sales/expense/bs_al/bs_equity）で矛盾判定。逆仕訳例外科目（売上値引・返品、仕入値引・返品）考慮。詳細は[16_valid_account_combinations.md](file:///c:/dev/receipt-app/docs/genzai/16_valid_account_combinations.md) | ✅ 2026-03-22 | バリデーション |
| 12 | `TAX_UNKNOWN` | 税区分不明 | 🔴 赤 | 全エントリのtax_category_idがnull or マスタ/顧問先設定に存在しない | ✅ 2026-03-24 | バリデーション |
| 13 | `DESCRIPTION_UNKNOWN` | 摘要不明 | 🔴 赤 | description === null \|\| description === '' | ✅ 2026-03-24 | バリデーション |
| 14 | `VOUCHER_TYPE_CONFLICT` | 証票意味矛盾 | 🟡 黄 | 証票意味（voucher_type）に基づくホワイトリスト方式。VOUCHER_TYPE_RULESテーブル参照。借方・貸方の科目がルールのallowedGroups/allowedIds/allowedCategoriesに該当しない場合 | ✅ 2026-03-27 | バリデーション |
| 15 | `TAX_ACCOUNT_MISMATCH` | 税区分科目矛盾 | 🟡 黄 | 科目のtaxDeterminationと設定された税区分の不一致。fixed→defaultTaxCategoryId不一致、auto_purchase→売上方向の税区分、auto_sales→仕入方向の税区分 | ✅ 2026-03-27 | バリデーション |
| 16 | `SAME_ACCOUNT_BOTH_SIDES` | 同一科目両建 | 🟡 黄 | 借方と貸方の両方に同一勘定科目IDが使用されている | ✅ 2026-03-27 | バリデーション |

**警告確認済み（warning_dismissals）**: 2026-03-28追加。ユーザーが「確認済み」とした警告タイプを`warning_dismissals: string[]`に記録。syncWarningLabelsCore再計算時にスキップ。

#### 不採用ラベル（旧定義から廃止）

| 旧ラベル | 旧定義 | 不採用理由 | 移行先 |
|---------|--------|-----------|--------|
| ~~`TAX_CALCULATION_ERROR`~~ | 税抜と消費税額の合計不一致 | 税額は計算しない設計に変更 | なし（削除） |
| ~~`MISSING_FIELD`~~ | 必須項目なし（証憑不備） | `on_document`（項目存在フラグ）に役割移譲 | `DATE_UNKNOWN` / `ACCOUNT_UNKNOWN` / `AMOUNT_UNCLEAR` に分離統合 |
| ~~`UNREADABLE_FAILED`~~ | 判読不能（読取失敗） | `on_document`（項目存在フラグ）に役割移譲 | 同上 |
| ~~`AMOUNT_ANOMALY`~~ | 高額アラート | 設定・ロジック過剰 | なし（削除） |
| ~~`OCR_FAILED`~~ | OCR失敗 | 2つの原因が混在 | `on_document`（項目存在フラグ）+ フィールド別ラベルに分離統合 |
| ~~`MISSING_RECEIPT`~~ | 証憑欠落 | ラベル名が意味不一致 | 同上 |
| ~~`OCR_LOW_CONFIDENCE`~~ | OCR信頼度低 | ラベル名が不正確 | `UNREADABLE_ESTIMATED` にリネーム・再定義 |
| ~~`HAS_MEMO`~~ | メモあり | 証票メモ検出は警告に昇格 | `MEMO_DETECTED`（警告#10）に昇格 |

#### LLM備忘: 層B判定ロジック

**1: 既存ロジック（そのまま使用）**
- 1: `checkDebitCreditMismatch()` — 借方合計 ≠ 貸方合計

**2/3/4: フィールド欠損判定（`on_document`（項目存在フラグ）× 値で判定）**

```
on_document（項目存在フラグ） | value（値）  | ラベル                          | ホバーメッセージ
false（項目なし）               | null（空）   | DATE_UNKNOWN等（赤）             | 「証憑に○○の記載がありません」
true（項目あり）                | null（空）   | DATE_UNKNOWN等（赤）             | 「○○の読み取りに失敗しました」
true（項目あり）                | あり（低信頼）| UNREADABLE_ESTIMATED（黄）      | 「判読困難（AI推測値）」
true（項目あり）                | あり（高信頼）| 正常                            | —
```

スキーマ変更必要:
- 追加済み: `date_on_document: boolean`（日付項目存在フラグ）, `amount_on_document: boolean`（金額項目存在フラグ）, `account_on_document: boolean`（勘定科目項目存在フラグ）

**5: ファイルハッシュ比較（新規ロジック）**
- 層B: 同バッチ内のファイルSHA256比較
- 層C: 過去DB内のファイルハッシュ照合

**6: 複数証票検出（Gemini出力そのまま）**
- 層A: `has_multiple_vouchers==true` → MULTIPLE_VOUCHERS（複数証票）
- 層Bロジック不要（Geminiの出力をそのままラベル変換）

**7: 条件一致重複検出（既存ロジック拡張）**
- 層B: `checkDuplicates()` — 同バッチ内で日付+金額+証票タイプ一致
- 層C: 過去DB照合（日付+金額+科目一致）
- 注意: 定期支払の誤検出を防ぐ除外ルール必要

**8: 日付範囲チェック（既存ロジック修正）**
- 現状: `checkDateAnomaly()` — 未来 or 1年以上前
- 修正: 会計期間外 or 未来日付に変更
- テスト用: `date < 2025-04-01 || date > 2026-03-31`
- 将来: 会計期間をテナント設定から取得

**10: 手書きメモ検出（Gemini出力そのまま）**
- 層A: `handwritten_flag==='MEANINGFUL'` → MEMO_DETECTED（手書きメモ検出）
- NON_MEANINGFUL/NONEは警告なし。角印・受領印・スタンプは手書きに含めない

### 証憑種類（7個）

```typescript
'RECEIPT'                  // レシート・領収証
'INVOICE'                  // 請求書
'TRANSPORT'                // 交通費
'CREDIT_CARD'              // クレジットカード
'BANK_STATEMENT'           // 銀行明細
'MEDICAL'                  // 医療費
'NOT_APPLICABLE'           // 仕訳対象外（名刺等）
```

### 制度系（3個）

```typescript
'INVOICE_QUALIFIED'        // 適格請求書
'INVOICE_NOT_QUALIFIED'    // 不適格請求書
'MULTI_TAX_RATE'           // 複数税率
```

### ルール（2個）

```typescript
'RULE_APPLIED'             // ルール適用済み
'RULE_AVAILABLE'           // ルール適用可能
```

### その他（0個 — 2026-02-23廃止）

> **⚠️ 2026-02-23変更**: HAS_MEMOは廃止。証票上の手書きメモ検出は警告ラベル#9 MEMO_DETECTEDに昇格。担当者メモの有無はコメント列（staff_notes.memo）で表示。

### 要対応（staff_notesに移行済み — 2026-02-21）

> **⚠️ 2026-02-21変更**: 要対応ステータスはlabels配列から`staff_notes`オブジェクトに移行。
> モック型定義: `src/mocks/types/staff_notes.ts`
> 本番ではstaff_notesテーブル分離 or JSONB化をPhase Cで決定。

```typescript
// labelsから削除済み。staff_notesで管理:
'NEED_DOCUMENT'            // 書類が不足
'NEED_INFO'                // 情報が不足（旧NEED_CONFIRMから改名）
'REMINDER'                 // 備忘メモ（新規追加）
'NEED_CONSULT'             // 社内相談する
```

### 出力制御（0個 — 2026-02-20判断: カラム管理に移行）

> **⚠️ Phase C変更予定**: `EXPORT_EXCLUDE` はラベルから廃止し、`export_exclude` カラム（BOOLEAN + CHECK制約）のみで管理する。
> 理由: ラベルは「特性（何であるか）」、export_excludeは「制御（どうするか）」。レイヤーが違う。
> 判断の詳細: task_current.md「2026-02-20確定: export_exclude設計判断」参照。

用例:
- 個人的な支出で会計に含めない仕訳
- 一時的な仮入力で出力したくない仕訳

### ラベル＋UI列 統合対応表（2026-02-23作成）

> 全UI列とラベルの対応関係を1つの表で管理する。①定義 ②コード ③テスト の進捗追跡を含む。

| # | UI列 | ラベル/キー | 日本語定義 | 色 | パイプライン | 判定ロジック | ① | ② | ③ |
|---|------|-----------|-----------|:--:|:---:|------------|:--:|:--:|:--:|
| 1 | 選 | （UI状態） | 一括操作用チェックボックス | — | — | UIのみ。行選択の状態管理 | — | — | — |
| 2 | No. | （UI生成） | 表示用連番 | — | — | UIが表示順に自動生成 | — | — | — |
| 3 | 写真 | image_url | 証票画像サムネイル | — | ✅ | 取込時のファイルパス/URLをjournals.image_urlに格納 | — | — | — |
| 4 | 過去仕訳 | （計算結果） | 類似する過去仕訳の有無 | — | ❌ | 過去仕訳DBと照合（層C必要・未着手） | 未 | 未 | 未 |
| 5-1 | 学習 | RULE_APPLIED | 学習ルール適用済み | — | ❌ | 学習フロー内で付与（層C・未着手）。ルール完全優先 | ✅ | 未 | 未 |
| 5-2 | 学習 | RULE_AVAILABLE | 学習ルール適用可能 | — | ❌ | 過去仕訳DBに類似パターンあり（層C・未着手）。排他的 | ✅ | 未 | 未 |
| 6-1 | コメント | NEED_DOCUMENT | 資料が必要 | — | — | staff_notesオブジェクト。担当者がUIで設定 | — | — | — |
| 6-2 | コメント | NEED_INFO | 情報が必要 | — | — | staff_notesオブジェクト。担当者がUIで設定 | — | — | — |
| 6-3 | コメント | REMINDER | リマインダー | — | — | staff_notesオブジェクト。担当者がUIで設定 | — | — | — |
| 6-4 | コメント | NEED_CONSULT | 相談が必要 | — | — | staff_notesオブジェクト。担当者がUIで設定 | — | — | — |
| 7 | 要対応 | （6-1〜6-4の状態） | 顧問先への確認要否トグル表示 | — | — | staff_notes各キーのenabled状態 | — | — | — |
| 8-1 | 証票 | RECEIPT | レシート・領収証 | — | ✅ | voucher_type='RECEIPT' | ✅ | ✅ | 未 |
| 8-2 | 証票 | INVOICE | 請求書 | — | ✅ | voucher_type='INVOICE' | ✅ | ✅ | 未 |
| 8-3 | 証票 | TRANSPORT | 交通費 | — | ✅ | voucher_type='TRANSPORT' | ✅ | ✅ | 未 |
| 8-4 | 証票 | CREDIT_CARD | クレジットカード | — | ✅ | voucher_type='CREDIT_CARD' | ✅ | ✅ | 未 |
| 8-5 | 証票 | BANK_STATEMENT | 銀行明細 | — | ✅ | voucher_type='BANK_STATEMENT' | ✅ | ✅ | 未 |
| 8-6 | 証票 | MEDICAL | 医療費 | — | ✅ | voucher_type='MEDICAL' | ✅ | ✅ | 未 |
| 8-7 | 証票 | NOT_APPLICABLE | 仕訳対象外 | — | ✅ | voucher_type='NOT_APPLICABLE' | ✅ | ✅ | 未 |
| 9-1 | 警告 | DEBIT_CREDIT_MISMATCH | 借方貸方の合計額不一致 | 🔴 | ✅ | 借方合計額 ≠ 貸方合計額 | ✅ | ✅ | 未 |
| 9-2 | 警告 | DATE_UNKNOWN | 日付が不明 | 🔴 | ✅ | `voucher_date === null`。`on_document`（項目存在フラグ）でホバー分岐 | ✅ | 未 | 未 |
| 9-3 | 警告 | ACCOUNT_UNKNOWN | 勘定科目が不明 | 🔴 | ✅ | `account === null`。`on_document`（項目存在フラグ）でホバー分岐 | ✅ | 未 | 未 |
| 9-4 | 警告 | AMOUNT_UNCLEAR | 内訳が不明瞭な金額あり | 🔴 | ✅ | `amount === null`。`on_document`（項目存在フラグ）でホバー分岐 | ✅ | 未 | 未 |
| 9-5 | 警告 | DUPLICATE_CONFIRMED | 完全重複：同一画像レベル | 🔴 | ✅ | ファイルハッシュ一致 | ✅ | 未 | 未 |
| 9-6 | 警告 | MULTIPLE_VOUCHERS | 複数の証票あり | 🔴 | ✅ | has_multiple_vouchers==true（Geminiが画像から検出） | ✅ | 未 | 未 |
| 9-7 | 警告 | DUPLICATE_SUSPECT | 重複疑い：定期支払等の可能性有 | 🟡 | ✅ | 日付+金額+証票タイプ一致 | ✅ | ✅ | 未 |
| 9-8 | 警告 | DATE_OUT_OF_RANGE | 会計期間外または未来日付 | 🟡 | ✅ | 伝票日が会計期間外 or 未来日付 | ✅ | ✅ | 未 |
| 9-9 | 警告 | UNREADABLE_ESTIMATED | 判読困難（AI推測値） | 🟡 | ✅ | 読みにくいがAIが推測で値を入れた | ✅ | 未 | 未 |
| 9-10 | 警告 | MEMO_DETECTED | 証票に手書きメモあり | 🟡 | ✅ | handwritten_flag==='MEANINGFUL'（会計的に意味のある手書きのみ。角印・スタンプ除外） | ✅ | 未 | 未 |
| 9-11 | 警告 | CATEGORY_CONFLICT | 貸借科目矛盾（5分類） | 🔴 | ✅ | 全借方×全貸方クロスチェック。accountGroup→5分類で矛盾判定。逆仕訳例外科目考慮 | ✅ | ✅ | 未 |
| 9-12 | 警告 | TAX_UNKNOWN | 税区分不明 | 🔴 | ✅ | tax_category_id===null or マスタ/顧問先設定に不在 | ✅ | ✅ | 未 |
| 9-13 | 警告 | DESCRIPTION_UNKNOWN | 摘要不明 | 🔴 | ✅ | description===null or 空文字 | ✅ | ✅ | 未 |
| 9-14 | 警告 | VOUCHER_TYPE_CONFLICT | 証票意味矛盾 | 🟡 | ✅ | VOUCHER_TYPE_RULESホワイトリスト方式。科目がルール範囲外 | ✅ | ✅ | 未 |
| 9-15 | 警告 | TAX_ACCOUNT_MISMATCH | 税区分科目矛盾 | 🟡 | ✅ | taxDeterminationと税区分direction不一致 | ✅ | ✅ | 未 |
| 9-16 | 警告 | SAME_ACCOUNT_BOTH_SIDES | 同一科目両建 | 🟡 | ✅ | 借方と貸方に同一科目IDが存在 | ✅ | ✅ | 未 |
| 9-17 | 警告 | ~~TAX_CALCULATION_ERROR~~ | 税抜と消費税額の合計不一致 | — | — | 税額は計算しない設計に変更 | ❌廃止 | — | — |
| 9-12 | 警告 | ~~MISSING_FIELD~~ | 必須項目なし（証憑不備） | — | — | `on_document`（項目存在フラグ）+ フィールド別ラベルに分離統合 | ❌廃止 | — | — |
| 9-13 | 警告 | ~~UNREADABLE_FAILED~~ | 判読不能（読取失敗） | — | — | 同上 | ❌廃止 | — | — |
| 9-14 | 警告 | ~~AMOUNT_ANOMALY~~ | 高額アラート | — | — | 勘定科目に対して金額が異常 | ❌不採用 | — | — |
| 9-15 | 警告 | ~~OCR_FAILED~~ | OCR失敗 | — | — | 画像から文字を読み取れなかった | ❌不採用 | — | — |
| 9-16 | 警告 | ~~MISSING_RECEIPT~~ | 証憑欠落 | — | — | 証憑画像が添付されていない | ❌不採用 | — | — |
| 9-17 | 警告 | ~~OCR_LOW_CONFIDENCE~~ | OCR信頼度低 | — | — | 文字の読み取り精度が低い | ❌不採用 | — | — |
| 9-18 | 警告 | ~~HAS_MEMO~~ | 担当者メモの有無 | — | — | 廃止。コメント列で代用 | ❌廃止 | — | — |
| 10 | クレ払い | is_credit_card_payment | クレジットカード払いか否か | — | ✅ | is_credit_card_payment==true（Geminiが画像から検出） | ✅ | 未 | 未 |
| 11 | 軽減 | MULTI_TAX_RATE | 軽減税率対象の有無 | — | ✅ | tax_entries[]に税率8%のエントリが存在 | ✅ | 未 | 未 |
| 12-1 | 適格 | INVOICE_QUALIFIED | 適格請求書 | — | ✅ | invoice_registrationがnullでない（登録番号あり） | ✅ | 未 | 未 |
| 12-2 | 適格 | INVOICE_NOT_QUALIFIED | 非適格請求書 | — | ✅ | invoice_registrationがnull（登録番号なし） | ✅ | 未 | 未 |
| — | 証票メモ | memo | 証票上の手書きメモ内容 | — | ✅ | Gemini出力のhandwritten_memo_contentを格納 | ✅ | 未 | 未 |
| 13 | 伝票日 | date | 証票の日付（伝票日） | — | ✅ | Gemini出力のdateを格納 | ✅ | 未 | 未 |
| 14 | 摘要 | description | 取引内容の要約 | — | ✅ | suggestions[].descriptionを格納 | ✅ | 未 | 未 |
| 15 | 借方勘定科目 | account | 借方の勘定科目名 | — | ✅ | suggestions[].account（entry_type='debit'） | ✅ | 未 | 未 |
| 16 | 借方補助 | sub_account | 借方の補助科目 | — | ❌ | Geminiスキーマに追加要（ギャップG1） | ✅ | 未 | 未 |
| 17 | 借方税区分 | tax_category_id | 借方の消費税課税区分（概念ID） | — | ❌ | Geminiスキーマに追加要（ギャップG2） | ✅ | 未 | 未 |
| 18 | 借方金額 | amount | 借方の金額 | — | ✅ | suggestions[].amount（entry_type='debit'） | ✅ | 未 | 未 |
| 19 | 貸方勘定科目 | account | 貸方の勘定科目名 | — | ✅ | suggestions[].account（entry_type='credit'） | ✅ | 未 | 未 |
| 20 | 貸方補助 | sub_account | 貸方の補助科目 | — | ❌ | Geminiスキーマに追加要（ギャップG1） | ✅ | 未 | 未 |
| 21 | 貸方税区分 | tax_category_id | 貸方の消費税課税区分（概念ID） | — | ❌ | Geminiスキーマに追加要（ギャップG2） | ✅ | 未 | 未 |
| 22 | 貸方金額 | amount | 貸方の金額 | — | ✅ | suggestions[].amount（entry_type='credit'） | ✅ | 未 | 未 |
| 23 | 操作 | 削除/編集/復元 | 行操作ボタン | — | — | UIのみ。DELETE API呼び出し | — | — | — |
| — | — | ~~EXPORT_EXCLUDE~~ | 出力除外 | — | — | Phase Cでexport_excludeカラム管理に移行 | ❌移行 | — | — |

---

### PostgreSQL実装

```sql
-- TEXT配列（GINインデックス可能）
ALTER TABLE journals
ADD COLUMN labels TEXT[] DEFAULT '{}';

-- GINインデックス（高速検索）
CREATE INDEX idx_journals_labels ON journals USING GIN(labels);
```

---

## 📖 未読・既読管理

### is_read フィールド

| フィールド | 型 | 説明 |
|-----------|----|----|
| `is_read` | boolean | 未読（false）/ 既読（true）|
| `read_at` | timestamp | 既読日時 |

### 既読になる条件

以下のいずれかの操作で自動的に既読になる:

1. **手動既読**: ユーザーが既読ボタンをクリック
2. **編集時**: 仕訳を編集した時
3. **詳細画面表示時**: 詳細画面を開いた時

### UI表示

- ゴミ箱: 背景色が濃グレー+白字（`bg-gray-600 text-white`）← **最優先**
- 出力済み: 背景色が薄グレー（`bg-gray-200`）
- 未読: 背景色が黄色（`bg-yellow-100`）
- 既読: 背景色が白（`bg-white`）

> **2026-02-20実装**: exported→灰を最優先とし、未読（黄）より優先する3色ロジックをモックに実装済み。
> **2026-02-21追加**: is_trashed（ゴミ箱）を最優先に追加。4色優先順位制に拡張。

### データベース実装

```sql
ALTER TABLE journals
ADD COLUMN is_read BOOLEAN NOT NULL DEFAULT FALSE,
ADD COLUMN read_at TIMESTAMP NULL;
```

---

## 🔒 3. readonly定義

### 基本式

```typescript
readonly = (status === 'exported')
```

### 理由

- **`exported` = CSV出力完了 = MFへ引き渡し完了**
- 本システムの責任範囲外
- 修正はMF側で実施

### Phase 5での拡張（予定）

```typescript
function isJournalEditable(
  journal: Journal, 
  context?: { periodClosed?: boolean; user?: User }
): boolean {
  if (journal.status === 'exported') return false;
  if (context?.periodClosed) return false;  // 期間締め
  if (!context?.user?.hasPermission('edit')) return false;  // 権限
  return true;
}
```

---

## 📋 4. journals テーブル完全スキーマ

### CREATE TABLE文

> **DDLの単一ソース**: [migration.sql](file:///C:/dev/receipt-app/docs/genzai/02_database_schema/journal/migration.sql) を参照。
> 本ファイルは設計思想と根拠を記載する。DDLの重複管理はドリフトの原因となるため行わない。

> **ID形式**: 2026-03-11に接頭辞+連番方式に統一。詳細は [12_full_schema_design_20260311.md](file:///c:/dev/receipt-app/docs/genzai/12_full_schema_design_20260311.md) §2を参照。
> 例: 仕訳=`jrn-00000001`、証票=`doc-00000001`、顧問先=`ABC-00001`

**テーブル構成**:
- `journals` — 仕訳ヘッダ（status, labels, メモ, 出力管理, ゴミ箱）
- `journal_entries` — N対N複合仕訳明細（借方/貸方分離）
- `export_batches` — CSV出力履歴
- `journal_exports` — 仕訳とバッチの紐付け（N対N）

**CHECK制約4つ**: exported同期, メモ作成者必須, 削除者必須, 出力対象外理由必須

**カラム詳細**: migration.sqlの`journals`テーブル定義を参照。

**合計**: 約25カラム（journals）+ journal_entriesでN対N明細管理

---

## 📊 5. インデックス設計（7個）

```sql
-- status検索（exported/未出力の判別）
CREATE INDEX idx_journals_status ON journals(status);

-- 未読一覧（黄色ハイライト）
CREATE INDEX idx_journals_is_read ON journals(is_read);

-- ゴミ箱一覧
CREATE INDEX idx_journals_deleted_at ON journals(deleted_at);

-- labels検索（GINインデックス）
CREATE INDEX idx_journals_labels ON journals USING GIN(labels);

-- 複合インデックス（背景色ロジック高速化）
CREATE INDEX idx_journals_status_read ON journals(status, is_read);

-- 出力履歴検索
CREATE INDEX idx_journals_exported_at ON journals(exported_at);

-- 作成日時順ソート
CREATE INDEX idx_journals_created_at ON journals(created_at);
```

**設計判断**:
- 部分インデックス廃止（WHERE句不要）
- 200社規模では維持コストは無視できる（約5秒/日）
- すべてのクエリパターンにインデックス作成

---

## 🎨 6. 背景色ロジック

### 一覧UI（4色・優先順位制）

```typescript
/**
 * 行背景色の優先順位
 *
 * 1. deleted_at!=null : 濃グレー+白字（ワークフロー終了・最優先）
 * 2. status=exported : 薄グレー（出力完了）
 * 3. !is_read        : 黄色（未読・注意）
 * 4. それ以外        : 白（通常）
 *
 * 制約: exported && deleted_at は禁止（trashJournalでガード）
 * 許可: export_exclude && deleted_at は許可（外部未出力のため）
 * フィルタ: showTrashed=ONは「追加表示型」（通常+ゴミ箱）
 */
function getRowBackground(journal: JournalPhase5Mock): string {
  // 優先度1: ゴミ箱 → 濃グレー+白字（最優先）
  if (journal.deleted_at !== null) {
    return 'bg-gray-600 text-white';
  }
  // 優先度2: 出力済み → 薄グレー
  if (journal.status === 'exported') {
    return 'bg-gray-200';
  }
  // 優先度3: 未読 → 黄色
  if (!journal.is_read) {
    return 'bg-yellow-100';
  }
  // 優先度4: 通常 → 白
  return 'bg-white';
}
```

- ⬛ 濃グレー+白字（ゴミ箱） = 「論理削除・ワークフロー終了」→ **最優先**
- 🔲 薄グレー（出力済み） = 「CSV出力完了」
- 🟨 黄色（未読） = 「まだ誰も見てない！」→ 業務未着手
- ⬜ 白（既読） = 「誰かが見た」→ 業務着手済み

> **2026-02-20変更**: 優先順位制を採用。exported→灰を最優先とすることで、
> 「未読かつ未出力」の競合を解消。出力済み行は常にグレーで視覚的に確定状態を示す。
> **2026-02-21変更**: is_trashed（ゴミ箱）を最優先に追加。3色→4色に拡張。
> モック実装済み（JournalListLevel3Mock.vue `getRowBackground`関数）。

### 過去仕訳モーダル（3色）

| 条件 | 背景色 | 色値 |
|---|---|---|
| status = 'exported' | 白 | #FFFFFF |
| status = null, export_exclude = false | 薄い青 | 未定 |
| export_exclude = true | 薄い赤 | 未定 |

> is_readはモーダルでは使用しない（過去仕訳参照で未読/既読は無関係）。

---

## 📋 7. UI列構成（26列）

> **列定義の単一ソース**: [journalColumns.ts](file:///C:/dev/receipt-app/src/mocks/columns/journalColumns.ts)（JournalColumn型定義付き、sortKey付き26列）を参照。
> **列幅リサイズ**: [useColumnResize.ts](file:///C:/dev/receipt-app/src/mocks/composables/useColumnResize.ts) composableで全テーブルページの列幅をドラッグ変更可能。`localStorage`に永続化。`table-layout: fixed` + `colgroup`動的バインド方式。仕訳一覧＋マスタ/顧問先設定ページ全10ページに適用済み（2026-03-19）。
> **2026-03-19変更**: D7aで借方/貸方「区分」列2列追加、2026-02-23の23列から26列に拡張。新type: `account-dropdown`, `category-dropdown`。

| 列 | key | type | データ | 操作 |
|----|-----|------|--------|------|
| 1 | select | checkbox | — | 一括操作 |
| 2 | no | index | display_order | — |
| 3 | photo | component | document_id | ホバーでプレビュー、ドラッグ移動可能モーダル |
| 4 | pastJournal | component | — | 過去仕訳検索、ドラッグ移動可能モーダル |
| 5 | comment | component | staff_notes | コメントモーダル（staff_notesベース4項目、ドラッグ移動可能）✅ 2026-02-21実装済 |
| 6 | needAction | component | staff_notes (4種) | 4FAアイコントグル（NEED_DOCUMENT/NEED_INFO/REMINDER/NEED_CONSULT）✅ 2026-02-21実装済 |
| 7 | labelType | component | labels (証票) | 証票種類表示 |
| 8 | warning | component | labels (事故) | 警告表示（警告ラベル16種+モック層バリデーション警告6種） |
| 9 | rule | component | labels (RULE_*) | 学習状態表示 |
| 10 | taxRate | component | labels (MULTI_TAX_RATE) | 軽減税率表示 |
| 11 | memo | component | memo | 証票メモホバー ✅ 2026-02-21実装済 |
| 12 | invoice | component | labels (INVOICE_*) | 適格判定表示 |
| 13 | voucher_date | text | voucher_date | — |
| 14 | description | text | description | — |
| 15 | debit.account | text | 借方勘定科目 | — |
| 16 | debit.sub_account | text | 借方補助 | — |
| 17 | debit.tax_category_id | text | 借方税区分（概念ID→マスタ名称変換表示） | — |
| 18 | debit.amount | amount | 借方金額 | — |
| 19 | credit.account | text | 貸方勘定科目 | — |
| 20 | credit.sub_account | text | 貸方補助 | — |
| 21 | credit.tax_category_id | text | 貸方税区分（概念ID→マスタ名称変換表示） | — |
| 22 | credit.amount | amount | 貸方金額 | — |
| 23 | actions | action | — | ⋯ドロップダウン（未読/既読・対象/対象外・コピー・ゴミ箱/復活） |

**一括操作**:
- コピー → 直下に未読で挿入。確認メッセージ「N件を未出力にコピーしますか？」
- 既読にする → `is_read = TRUE`（黄色→白）
- 未読にする → `is_read = FALSE`（備忘録）
- 出力対象外 → `export_exclude = TRUE`
- ゴミ箱 → `deleted_at = NOW()`

**exported行の操作制限（2026-02-21確定）**:
- 個別ドロップダウン: 未読/既読・対象/対象外をdisabled+グレーアウト（ゴミ箱と同様の見た目）
- 一括操作: exported行はスキップ方式。全件exportedなら「実行不可」メッセージ表示
  - メッセージ例: 「実行不可 選択: 2件 / 出力済み: 2件（スキップ） 実行可能な仕訳がありません。」
  - 混在時: exportedをスキップし、残りの未出力分のみ実行
- コピー: exported行もコピー可能（コピー先は常にstatus=null）
  - 個別メッセージ: 「○○を未出力にコピーしますか？」
  - 一括メッセージ: 「N件を未出力にコピーしますか？」

---

## 📦 8. export管理テーブル

### export_batches テーブル

```sql
CREATE TABLE export_batches (
  id VARCHAR(20) PRIMARY KEY,        -- 例: batch-20260311-01
  client_id VARCHAR(20) NOT NULL,     -- 例: ABC-00001
  exported_at TIMESTAMP NOT NULL DEFAULT NOW(),
  exported_by VARCHAR(20) NOT NULL,   -- 例: staff-0001
  journal_count INTEGER NOT NULL,
  filename TEXT NOT NULL
);
```

> **ID形式変更（2026-03-19）**: UUIDから接頭辞+連番方式に統一。[12_full_schema_design](file:///c:/dev/receipt-app/docs/genzai/12_full_schema_design_20260311.md) §2準拠。

**用途**: CSV出力の履歴管理（いつ、誰が、何件）

### journal_exports テーブル

```sql
CREATE TABLE journal_exports (
  id VARCHAR(20) PRIMARY KEY,
  journal_id VARCHAR(20) NOT NULL REFERENCES journals(id),
  export_batch_id VARCHAR(20) NOT NULL REFERENCES export_batches(id),
  exported_at TIMESTAMP NOT NULL DEFAULT NOW(),
  UNIQUE(journal_id, export_batch_id)
);
```

**用途**: 仕訳とバッチの紐付け（N対N）

---

## 🔄 9. CSV出力フロー

> **実装状況（2026-03-27）**: MF CSVエクスポートが[exportMfCsv.ts](file:///c:/dev/receipt-app/src/mocks/utils/exportMfCsv.ts)に実装済み。
> - 21列MF仕訳帳CSV準拠（`MfCsvRow`型で定義）
> - 複合仕訳（N対N）展開対応（`expandJournalToMfRows()`）
> - CSV出力前バリデーション（`validateForCsvExport()`）: 警告ラベル付き仕訳を自動除外
> - UTF-8 BOM付き出力（`downloadMfCsv()`）
> - 出力バッチID追跡（`export_batch_id`フィールドに記録）
> - ID→MF名称変換はuseAccountSettings経由のコールバック方式

### 処理手順

```typescript
async function exportToCSV(clientId: string, userId: string) {
  // 1. 未出力のみ取得（exportedは含まない）
  const journals = await db.journals.findMany({
    where: { 
      clientId,
      status: null,  // 未出力のみ（exported以外）
      export_exclude: false  // 出力対象外を除外
    }
  });
  
  // 2. CSV生成
  const csv = generateMFCSV(journals);
  
  // 3. ファイル名（日時を含む）
  const timestamp = format(new Date(), 'yyyyMMdd_HHmmss');
  const filename = `${clientId}_${timestamp}_journals.csv`;
  
  // 4. ダウンロード
  downloadFile(csv, filename);
  
  // 5. 即座に exported に遷移
  await db.journals.updateMany(
    { id: { in: journals.map(j => j.id) } },
    { 
      status: 'exported',
      exported_at: new Date(),
      exported_by: userId
    }
  );
  
  // 6. バッチ記録
  await db.exportBatches.create({
    data: {
      clientId,
      exportedBy: userId,
      journalCount: journals.length,
      filename
    }
  });
  
  return { filename, count: journals.length };
}
```

### ファイル名規則

```
{client_id}_{yyyyMMdd_HHmmss}_journals.csv
例: clientA_20261211_143022_journals.csv
```

---

## 🛡️ 10. API層でのガード句（必須）

### 編集防止

```typescript
export async function updateJournal(
  journalId: string, 
  updates: Partial<Journal>,
  context: { userId: string }
): Promise<Journal> {
  const journal = await getJournal(journalId);
  
  // exported は編集不可
  if (journal.status === 'exported') {
    throw new BusinessRuleError(
      'CSV出力済みの仕訳は編集できません。' +
      'MF側で修正するか、管理者に問い合わせてください。',
      'EXPORTED_JOURNAL_READONLY'
    );
  }
  
  // ゴミ箱復元
  if (journal.deleted_at && !isRestoreRequest) {
    throw new Error('ゴミ箱内の仕訳は編集不可');
  }
  
  return await db.journals.update(journalId, updates);
}
```

---

## 🎨 11. UI表示仕様

### status表示

UI上では`status`は直接表示されません。

| status | 意味 | UI上の影響 |
|--------|------|------------| 
| `null` | 未出力（デフォルト） | 編集可能 |
| `exported` | 出力済み | 編集不可（出力列で表示。一覧UIの背景色はis_readのみ） |

### Composable実装

```typescript
// composables/useJournalStatus.ts
export function useJournalStatus(status: JournalStatus | null) {
  const isEditable = computed(() => status !== 'exported');
  const displayStatus = computed(() => status === 'exported' ? '出力済み' : '編集可能');
  
  return { isEditable, displayStatus };
}
```

---

## 📊 12. Streamedとの比較

### Streamedから採用した設計

| 項目 | Streamedの設計 | 本システムでの採用 |
|------|---------------|--------------------|
| 出力=完了 | 出力したら編集不可 | ✅ `status = 'exported'`で確定 |
| シンプルUI | ボタンを押させない | ✅ 自動保存、明示的な完了ボタンなし |
| ゴミ箱30日 | 論理削除→30日後物理削除 | ✅ `deleted_at` + バッチ処理 |
| 背景色管理 | 未読/既読で色分け | ✅ 黄色/白（2色。出力状態は出力列で表示） |
| コピー挙動 | 未読で直下に挿入 | ✅ `is_read = FALSE` |

### 本システム独自の要件

| 項目 | 本システムの必要性 | 実装方法 |
|------|--------------------|----------|
| 協力型フロー | 複数人で助け合う | ✅ staff_notes (NEED_DOCUMENT, NEED_INFO, REMINDER, NEED_CONSULT) |
| メモ機能 | サポート要請時の詳細記録 | ✅ memo, memo_author, memo_target |
| 出力対象外 | CSV出力しない意思表示（人間の業務判断） | ✅ export_excludeカラム（Phase Cでラベル廃止） |

---

## ⏰ 13. Phase 5 vs 遠い将来の切り分け

### Phase 5（今すぐ実装）

| 項目 | 実装 |
|------|------|
| status 1つの型定義（exported + null） | ✅ |
| label 18個の型定義（要対応3種はstaff_notesに移行。Phase CでEXPORT_EXCLUDE廃止後17個） | ✅ |
| タイムスタンプカラム | ✅ |
| 未読/既読カラム | ✅ |
| メモ機能カラム | ✅ |
| 出力管理カラム | ✅ |
| export管理テーブル | ✅ |
| readonly API層ガード | ✅ |
| CHECK制約4つ | ✅ |
| インデックス7個 | ✅ |

### 遠い将来（MF API連携後）

| 項目 | 内容 |
|------|------|
| MF差分検知 | API連携、ハッシュ比較 |
| 双方向同期 | MF側修正の自動取り込み |

---

## 🚨 14. 重要な設計判断

### ✅ 確定事項

1. **exported は編集不可**（Phase 5から厳格）
2. **MFが会計の真実**（本システムは業務レイヤー）
3. **完全同期しない**（差分検知のみ）
4. **CSV出力 = バッチ管理**（1仕訳ずつではない）
5. **ファイル名に日時含む**（重複防止）
6. **協力型フロー**（階層型承認フローではない）
7. **Streamed準拠**（出力=完了、背景色管理、ゴミ箱30日）

### ❌ やらないこと

1. ❌ MF側の修正を自動同期
2. ❌ exported の自動巻き戻し
3. ❌ 仕訳番号の厳密管理
4. ❌ CSV差分管理
5. ❌ リアルタイム双方向同期
6. ❌ 階層型承認フロー
7. ❌ 保存時の税区分期間バリデーション（UI選択制御で十分）
8. ❌ 取引日・計上日の分離管理（伝票日1本で十分）
9. ❌ MF API連携による勘定科目/税区分の自動取得（2026-03-20確認: パートナー限定API。一般開発者は利用不可）
10. ❌ CSV出力時の複雑なバリデーション（警告ラベルによる自動除外で十分）

---

## ✅ 15. 完成条件

Phase 5実装完了の定義：

- [ ] status 1つの型定義実装（exported + null）
- [ ] label 17個の型定義実装（要対応3種はstaff_notes移行済。Phase CでEXPORT_EXCLUDE廃止後）
- [ ] journals テーブルに全カラム追加
- [ ] export_batches / journal_exports テーブル作成
- [ ] API層にexportedガード句実装
- [ ] CSV出力時の自動遷移実装
- [ ] UI層にstatus表示実装
- [x] 背景色ロジック実装（4色優先順位制: ゴミ箱→濃灰 > exported→薄灰 > 未読→黄 > 既読→白。モック実装済み 2026-02-21）
- [ ] CHECK制約4つ実装
- [ ] インデックス7個実装

---

**Status**: 設計確定 ✅（定義B準拠）  
**Version**: v2.5（2026-03-29 警告ラベル6種追加・MF CSVエクスポート実装・useAccountSettingsフォールバック内部化・ScreenS_AccountSettings.vue削除）  
**Next**: Phase 5実装開始
