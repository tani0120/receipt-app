# null許容化・項目存在フラグ・警告ラベル整備計画

> 作成日: 2026-03-04
> 根拠: STREAMED（ストリームド）UI調査、ChatGPT設計議論（2026-03-03）、journal_v2_20260214.md
> 承認: 2026-03-04 チャットにて承認済み

---

## 承認済みの設計決定

| 項目 | 決定 |
|------|------|
| `account`（勘定科目）のnull許容化 | ✅ 承認。nullの場合「未確定」と表記 |
| `amount`（金額）のnull許容化 | ✅ 承認。存在しなければ空白。存在すれば不明瞭でも貸借を合わせるよう類推 |
| `description`（摘要）のnull許容化 | ❌ 不要。nullは返さない |
| `on_document`（項目存在フラグ） | ✅ 持たせる。プロンプト（AI指示文）にも必須 |
| 列名変更 | 取引日 → **日付** |
| 警告ホバー | 3種: 「未入力項目あり」「内訳が不明瞭な金額あり」「日付が不明」 |
| CSV出力 | 人間が修正前提。可否は後日MF仕様確認 |

---

## null × on_document（項目存在フラグ） × 警告ラベル 相関表

| on_document（項目存在） | 値 | 警告ラベル | 日本語 | ホバーメッセージ | 色 |
|:---:|:---:|------|------|------|:---:|
| false（項目なし） | null | `MISSING_FIELD` | 必須項目なし | 「未入力項目あり」 | 🔴赤 |
| true（項目あり） | null | `UNREADABLE_FAILED` | 判読不能 | 「日付が不明」「内訳が不明瞭な金額あり」 | 🔴赤 |
| true（項目あり） | 値あり（低信頼度） | `UNREADABLE_ESTIMATED` | 判読困難（AI推測値） | 「内訳が不明瞭な金額あり。ご確認ください。」 | 🟡黄 |
| true（項目あり） | 値あり（高信頼度） | — | 正常 | — | — |

### フィールド別の適用

| フィールド（日本語名） | null時の一覧UI表示 | ホバーメッセージ |
|---------|------------|---------|
| `transaction_date`（日付） | 「未確定」 | 「日付が不明」 |
| `account`（勘定科目） | 「未確定」 | 「未入力項目あり」 |
| `amount`（金額） | 空白（存在しない場合）/ 類推値（存在する場合） | 「内訳が不明瞭な金額あり」 |

---

## 全作業項目（27件）

### 優先度1: 型定義の整合（他の全作業の前提）

| # | 対象ファイル | 作業内容 | 状態 |
|---|------------|---------|:---:|
| A1 | `src/domain/types/journal.ts`（ドメイン層の型定義） | `JournalEntryLine`（仕訳明細行）に `account_on_document`（勘定科目項目存在フラグ）、`amount_on_document`（金額項目存在フラグ）を追加 | ⬜ |
| A2 | `src/mocks/types/journal_phase5_mock.type.ts`（モック層の型定義） | `date_on_document`（日付項目存在フラグ）を追加 | ⬜ |
| A3 | `src/shared/definitions/field-nullable-spec.ts`（null許容定義表） | 承認内容に修正: `description`（摘要）除外、`on_document`（項目存在フラグ）追加、警告3種定義 | ⬜ |

### 優先度2: 警告ラベルの整備（UIと相関表の整合）

| # | 対象ファイル | 作業内容 | 状態 |
|---|------------|---------|:---:|
| B1 | `journal_v2_20260214.md`（仕訳スキーマ定義書）§2 | 警告ラベル定義に `on_document`（項目存在フラグ）との判定マトリクスを正式統合 | ⬜ |
| B2 | `field-nullable-spec.ts`（null許容定義表） | `warningLabel`（警告ラベル）を `on_document`（項目存在フラグ）による2種分岐に対応 | ⬜ |

### 優先度3: 一覧UIの変更

| # | 対象ファイル | 作業内容 | 状態 |
|---|------------|---------|:---:|
| D1 | `journalColumns.ts`（列定義ファイル） | 列名変更: 取引日 → 日付 | ⬜ |
| D2 | `JournalListLevel3Mock.vue`（仕訳一覧UI） | null表示: 日付・勘定科目がnullの場合「未確定」と表記 | ⬜ |
| D3 | 同上 | null表示: 金額がnullの場合「空白」表示 | ⬜ |
| D4 | 同上 | 警告列のホバー: 3種のメッセージ表示 | ⬜ |
| D5 | 同上 | ソート: null（空値）の場合、昇順=末尾、降順=先頭（`compareWithNull`（null比較関数）組込み） | ⬜ |
| D6 | 同上 | 金額null時の貸借合計計算: null行をスキップまたは0扱いする安全ロジック | ⬜ |

### 優先度4: フィクスチャ（テストデータ）の更新

| # | 対象ファイル | 作業内容 | 状態 |
|---|------------|---------|:---:|
| I1 | `journal_test_fixture_30cases.ts`（30件テストデータ） | null値を含むテストケース追加（日付null、勘定科目null、金額null各1件以上） | ⬜ |
| I2 | 同上 | `on_document`（項目存在フラグ）の値をテストデータに追加 | ⬜ |

### 優先度5: Geminiプロンプト（AI指示文）の変更

| # | 対象ファイル | 作業内容 | 状態 |
|---|------------|---------|:---:|
| G1 | Geminiプロンプト定義ファイル | `account`（勘定科目）: 「判定できない場合はnullを返せ」 | ⬜ |
| G2 | 同上 | `amount`（金額）: 「読み取れない場合はnullを返せ」 | ⬜ |
| G3 | 同上 | `on_document`（項目存在フラグ）: 「その項目が証憑に存在するかどうかも返せ」を追加 | ⬜ |
| G4 | 同上 | `transaction_date`（日付）: 「読み取れない場合はnullを返せ」 | ⬜ |
| G5 | 同上 | 信頼度（confidence）: 推測で入れた値には低信頼度マークを付けさせる | ⬜ |

### 優先度6: 金額類推ロジック

| # | 対象ファイル | 作業内容 | 状態 |
|---|------------|---------|:---:|
| F1 | 新規作成 | 金額がnullだが項目存在する場合、貸借を合わせるよう既知金額から不明瞭分を類推するロジック | ⬜ |

### 優先度7: CSV出力

| # | 対象ファイル | 作業内容 | 状態 |
|---|------------|---------|:---:|
| H1 | `mf-csv-date.ts`（MF CSV日付変換ユーティリティ） | ✅ 作成済み | ✅ |
| H2 | 新規作成 | CSV出力前にnull（空値）フィールドの件数を集計し、警告を表示するロジック | ⬜ |
| H3 | `journal_v2_20260214.md`（仕訳スキーマ定義書）§9 | CSV出力前警告の方針をドキュメントに記載 | ⬜ |

### 優先度8: 個別仕訳ページ（画面未着手）

| # | 対象ファイル | 作業内容 | 状態 |
|---|------------|---------|:---:|
| E1 | 未作成（個別仕訳編集画面） | 未入力項目を警告表示 | ⬜ |
| E2 | 同上 | 空白フィールドに「必須」と薄く表示（プレースホルダー） | ⬜ |

### 優先度9: ドキュメント最終更新

| # | 対象ファイル | 作業内容 | 状態 |
|---|------------|---------|:---:|
| J1 | `journal_v2_20260214.md`（仕訳スキーマ定義書） | null許容化・on_document・警告ラベル相関を正式反映 | ⬜ |

---

## 優先度の根拠

| 優先度 | 理由 |
|:---:|------|
| 1（型定義） | 他の全作業がこの型に依存する。最初に固めないと手戻り |
| 2（警告ラベル） | 型と警告の相関が整合しないとUI実装が矛盾する |
| 3（一覧UI） | ユーザーが触れる部分。型が固まったらすぐ反映 |
| 4（テストデータ） | UI変更の動作確認にnullデータが必要 |
| 5（プロンプト） | Gemini出力スキーマが型に依存する。型確定後に変更 |
| 6（金額類推） | 設計が必要。一覧UIの基本表示が先 |
| 7（CSV出力） | 当分先。方針記載のみ先行 |
| 8（個別仕訳ページ） | 画面自体が未着手 |
| 9（ドキュメント） | 全作業完了後に最終更新 |

---

## STREAMED（ストリームド）のnull表示（調査結果）

| 状態 | STREAMEDの表示 |
|------|---------------|
| 値なし（空欄） | `「-」` または空白 |
| 勘定科目未確定 | `「未確定」` テキスト |
| 読み取り不能（重大） | 🔴 赤アイコン（❗） |
| 読み取り不能（軽微） | 🟡 黄アイコン（⚠） |
| フィルタ検索 | 「空欄」で検索・抽出可能 |
