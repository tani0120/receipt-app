# AIコマンド機能 — 設計定義書

> 作成: 2026-05-20
> 準拠: `load_context.md` L130: ロジックはAPI側に書け
> 前提: 34_mf_mcp_integration.md（MF MCP連携基盤）
> AI基盤: Vertex AI（Gemini）— GCP統一
> LLM翻訳層: バックエンド内（Hono）で完結

---

## 1. 概要

sugu-sru上で自然言語の指示を受け付け、MF MCPツール呼び出し・内部データ操作・AI相談を実行する。
将来的にChrome拡張（Chatwork連携）への展開を見据え、UIコンポーネントを再利用可能に設計する。

---

## 2. 全設計決定事項

### 2-1. アーキテクチャ

| 項目 | 決定 |
|---|---|
| LLM翻訳層の配置 | **バックエンド内（Hono）**。フロントはAPIを叩くだけ |
| AI基盤 | **Vertex AI（Gemini 2.0 Flash）** |
| WRITE操作 | **確認モーダル付きで許可**（AIが提案 → 人間が承認 → 実行） |
| ストリーミング応答 | **SSE**（文字がポロポロ出る） |
| 言語 | **日本語のみ** |

### 2-2. AIが使えるツールの範囲

**MFツール19個 + sugu-sru内部API** の両方を含む。

#### MF MCPツール（19個）

| ツール | 種別 | 用途 |
|---|---|---|
| `mfc_ca_currentOffice` | READ | 事業者情報 |
| `mfc_ca_getTermSettings` | READ | 会計年度設定 |
| `mfc_ca_getAccounts` | READ | 勘定科目一覧 |
| `mfc_ca_getTaxes` | READ | 税区分一覧 |
| `mfc_ca_getJournals` | READ | 仕訳一覧 |
| `mfc_ca_getJournalById` | READ | 仕訳個別 |
| `mfc_ca_getTradePartners` | READ | 取引先一覧 |
| `mfc_ca_getDepartments` | READ | 部門一覧 |
| `mfc_ca_getSubAccounts` | READ | 補助科目一覧 |
| `mfc_ca_getConnectedAccounts` | READ | 連携サービス一覧 |
| `mfc_ca_getReportsTrialBalanceBalanceSheet` | READ | 残高試算表（BS） |
| `mfc_ca_getReportsTrialBalanceProfitLoss` | READ | 残高試算表（PL） |
| `mfc_ca_getReportsTransitionBalanceSheet` | READ | 推移表（BS） |
| `mfc_ca_getReportsTransitionProfitLoss` | READ | 推移表（PL） |
| `mfc_ca_en_ja_dictionary` | READ | 英日辞書 |
| `mfc_ca_postJournals` | **WRITE** | 仕訳作成 |
| `mfc_ca_putJournals` | **WRITE** | 仕訳更新 |
| `mfc_ca_postTradePartners` | **WRITE** | 取引先作成 |
| `mfc_ca_postTransactions` | **WRITE** | 明細作成 |

#### sugu-sru内部API

| API | 種別 | 用途 |
|---|---|---|
| 顧問先検索（`clientRoutes`） | READ | 顧問先をキーワードで検索 |
| 顧問先詳細（`clientRoutes`） | READ | 顧問先の基本情報・契約情報 |
| 見込先一覧（`leadRoutes`） | READ | 見込先の一覧・詳細 |
| 仕訳一覧（`journalRoutes`） | READ | sugu-sru側の仕訳データ |
| スタッフ一覧（`staffRoutes`） | READ | スタッフ情報 |
| MF認証状態（`mfAuthRoutes`） | READ | MF連携状態の確認 |
| MFデータ整合チェック | READ | sugu-sru ↔ MF のデータ比較 |
| 進捗管理（`progressRoutes`） | READ | 顧問先ごとの進捗状況 |

### 2-3. 応答形式

**テキスト・テーブル・アクションボタンの全形式に対応。**

```
AI応答の構造:
{
  type: 'text' | 'table' | 'action' | 'mixed',
  content: string,          // テキスト本文（Markdown対応）
  table?: { headers, rows }, // テーブルデータ
  actions?: [               // アクションボタン
    { label: '仕訳を作成', command: 'mf_post_journal', params: {...}, requireConfirm: true },
    { label: '詳細を見る', command: 'mf_get_journal', params: {...}, requireConfirm: false },
  ],
  suggestions?: string[],   // 次の操作候補（AIが提案）
}
```

### 2-4. 会話履歴

| 項目 | 仕様 |
|---|---|
| 保存先 | **DB永続化**（Supabase移行時にテーブル化。移行前はJSON） |
| 共有範囲 | **スタッフ全員**で閲覧可能 |
| 目的 | 質問方法・やれることの共有。ナレッジベース的活用 |
| 保存項目 | 入力テキスト、AI応答、実行されたツール、結果、タイムスタンプ、ユーザー名 |

### 2-5. 認証・権限

| ユーザー種別 | AIコマンド利用 |
|---|---|
| スタッフ（管理者） | ✅ 全機能 |
| スタッフ（一般） | ✅ 全機能 |
| ゲスト（顧問先） | ❌ sugu-sruにアクセスできないため対象外 |

### 2-6. AIのトーン

**丁寧だが堅すぎない。AI初心者のスタッフが安心して使えるレベル。**

```
❌ NG（堅すぎ）: 「お問い合わせいただきありがとうございます。以下の結果をご報告申し上げます。」
❌ NG（ぞんざい）: 「はい、やった。結果これ。」
✅ OK: 「テスト法人の勘定科目を取得しました。全45科目あります。
         気になる科目があれば、科目名で絞り込めますよ。」
✅ OK: 「すみません、その指示はちょっとわかりませんでした。
         たとえば「○○社の仕訳を見せて」のように指示してみてください。」
```

**禁止事項:**
- 想像・推測で回答しない。わからないときは正直に「わかりません」と言う
- データがない場合に架空のデータを返さない
- エラー時に原因を推測しない（事実のみ報告）

### 2-7. コスト管理

| 項目 | 仕様 |
|---|---|
| 設定場所 | `http://localhost:5173/#/admin-dashboard?tab=cost` |
| **分類** | **仕訳操作**（画像AIOCR → 仕訳作成の一連フロー）と **質問・MCP**（READ/WRITE問わずMCPへの問い合わせ・操作全般）の2分類 |
| 上限単位 | **月額上限**（円 or リクエスト数） |
| 上限超過時 | AIコマンドの実行を停止し「今月の利用上限に達しました」と表示 |
| ダッシュボード表示 | 今月の利用状況（グラフ）、残りの利用可能数、利用履歴 |

> **注意**: 仕訳操作とMCP質問は別カウント。仕訳操作の上限に達してもMCP質問は使える（逆も同様）。

### 2-8. エラーハンドリング

| エラー種別 | 対応 |
|---|---|
| **システムエラー**（MCP接続失敗、API通信エラー等） | 管理者にコピペ可能なエラーコード・ログを表示。「このエラーコードを管理者に共有してください: `ERR-MCP-403-20260520T1130`」 |
| **AI解析不能**（意図がわからない） | 「すみません、その指示はわかりませんでした。こんな指示ならできます：」+ 候補を提示 |
| **データなし** | 「該当するデータが見つかりませんでした。」（想像で埋めない） |

### 2-9. 操作ログ（2つの役割）

操作ログには**2つの独立した目的**がある：

#### 役割1: 再実行可能ログ（ナレッジ共有）

ログ一覧からクリックすると**同じ操作を再実行**できる。
「あの人がやっていた操作を自分もやりたい」「この前の集計をもう一度」に対応。
スタッフ全員の操作ログが閲覧可能（会話履歴と同様にナレッジベース的活用）。

#### 役割2: WRITE操作の修復ログ（安全網）

**Gemini 2.5はミスする**。WRITE操作（仕訳作成・取引先登録等）の結果を詳細に記録し、
ミスが発覚した場合に**修復（取消・差戻し）できるようにする**。

| 記録項目 | 内容 |
|---|---|
| タイムスタンプ | 実行日時 |
| ユーザー | 実行したスタッフ名 |
| 入力テキスト | ユーザーの自然言語入力 |
| 解析結果 | AIが判定したツール名・パラメータ |
| 実行結果 | 成功/失敗・レスポンス要約 |
| WRITE操作の場合 | 確認モーダルの承認/拒否 |
| **WRITE結果の詳細** | **作成された仕訳ID・取引先コード等。修復時に使用** |
| **WRITE前の状態** | **更新操作の場合、変更前のデータスナップショット** |
| 再実行ボタン | ✅ ログからワンクリックで同じ操作を再実行 |
| 修復ボタン | ✅ WRITE操作の場合、取消・差戻しを実行 |

### 2-10. コンテキスト認識（AIが「今何を見ているか」を理解する）

AIは以下の4つのコンテキストを認識して操作・回答できる：

| コンテキスト | 取得方法 | 使用例 |
|---|---|---|
| **現在のページ** | Vue Routerの`route.path` + `route.params` | 「このページの顧問先のMF状態を確認」→ 顧問先詳細ページなら自動でclientIdを取得 |
| **表示されているUI** | ページコンポーネントが公開するデータ（テーブルの行数、表示中の仕訳一覧等） | 「この一覧をCSVにして」→ 表示中のテーブルデータを取得 |
| **システム全体** | sugu-sru内部API（全顧問先、全仕訳等） | 「全顧問先のMF連携状態を確認」→ 全体を横断して取得 |
| **ユーザーが選択したテキスト** | `window.getSelection()` | テキスト選択 → 右クリック「AIに聞く」→ 選択部分を質問に含める |

コンテキストの優先順位：**選択テキスト > 現在のページ > システム全体**

これにより「この顧問先の」を省略して「仕訳見せて」だけで動く。

---

### 2-11. AIによる提案（カスタムコマンドの代替）

ユーザーがコマンドを定義するのではなく、**AIが「できること」を提案する。**

```
ユーザー: 「テスト法人について何かわかる？」

AI: 「テスト法人（株式会社すぐする）について、以下のことができます：

  [勘定科目を確認]  [今期の仕訳一覧]  [決算月の整合チェック]
  [税区分を確認]    [取引先一覧]      [残高試算表を取得]

  気になるものをクリックするか、自由に質問してください。」
```

ボタンクリック or 自然言語入力の両方で実行できる。

---

### 2-12. 2モード設計（AIの裁量最小化）

> **大前提: AIはブレる。嘘をつく。**
> だからAIの裁量を最小化する。AIには「結果を日本語にまとめる」だけやらせる。

#### 定型対応（本命）

- **パターンマッチ**でユーザーの意図をコード側で分類
- **ツール名・引数はコードで確定**（AIに聞かない）
- AIは「MCP実行結果のJSON → 人間が読める日本語」にするだけ
- AIの裁量ゼロ → ブレようがない

```
ユーザー: 「先月の仕訳を見せて」

sugu-sru（コード）:
  ① パターンマッチ → journals_period にヒット
  ② 日付計算 → start_date: "2026-04-01", end_date: "2026-04-30"
     ※ AIではなくコードが計算
  ③ mfc_ca_getJournals({ start_date, end_date }) を実行
  ④ 結果JSONを取得

AI（要約だけ）:
  「4月の仕訳は12件です。
   | 日付 | 借方 | 貸方 | 金額 | 摘要 |
   | 4/1 | 旅費交通費 | 事業主借 | 500 | ... |
   ...」

→ AIはツール選定にも引数にも関与していない。要約しただけ。
```

#### 定型パターン一覧

| パターンID | マッチキーワード例 | 実行するツール | 引数の決め方 |
|---|---|---|---|
| `office_info` | 事業者情報、会社情報 | `mfc_ca_currentOffice` | 引数なし |
| `accounts_list` | 勘定科目、科目一覧 | `mfc_ca_getAccounts` | `available: true` |
| `taxes_list` | 税区分 | `mfc_ca_getTaxes` | `available: true` |
| `journals_period` | 仕訳、ジャーナル、取引 | `mfc_ca_getJournals` | 日付計算はコード |
| `journal_detail` | 仕訳ID xxx | `mfc_ca_getJournalById` | IDをパース |
| `pl_trial` | PL試算表、損益計算書 | `getReportsTrialBalanceProfitLoss` | `fiscal_year` をコードで算出 |
| `bs_trial` | BS試算表、貸借対照表 | `getReportsTrialBalanceBalanceSheet` | 同上 |
| `pl_transition` | PL推移、月別推移 | `getReportsTransitionProfitLoss` | `type: "monthly"` 固定 |
| `bs_transition` | BS推移 | `getReportsTransitionBalanceSheet` | `type: "monthly"` 固定 |
| `partners_list` | 取引先 | `mfc_ca_getTradePartners` | — |
| `departments` | 部門 | `mfc_ca_getDepartments` | — |
| `sub_accounts` | 補助科目 | `mfc_ca_getSubAccounts` | — |
| `connected` | 連携サービス、銀行口座 | `mfc_ca_getConnectedAccounts` | — |
| `term_settings` | 会計年度、決算月 | `mfc_ca_getTermSettings` | — |
| `data_compare` | MFとの差分、データ比較 | `currentOffice` + `sugusru_getClient` | 両方実行して差分 |

#### オープン対応（フォールバック）

- 定型パターンにマッチしなかった場合**のみ**発動
- **やむを得ない**のでAIにFunction Callingの裁量を与える
- AIに34_/35_の関連セクションを検索させて（`search_design_docs`）、自分でツールを選ばせる
- ブレるリスクがあるが、「対応できません」より良い

```
ユーザー: 「不動産の利回りは適正か？」

sugu-sru:
  ① パターンマッチ → マッチなし → オープン対応
  ② AIに以下を渡す:
     - ユーザーの質問
     - search_design_docs ツール（設計書検索）
     - MCPツール19個のFunction Calling定義
     - テナント情報

AI（自律判断）:
  Step 1: search_design_docs("不動産 利回り") → 設計書を読む
  Step 2: mfc_ca_getReportsTrialBalanceProfitLoss() → PL取得
  Step 3: 賃貸料 ÷ 不動産関連経費 で分析
  Step 4: 回答

→ AIが自律的に判断。正しいかもしれないし、間違うかもしれない。
```

#### AIの裁量の範囲まとめ

| 段階 | 定型対応 | オープン対応 |
|---|---|---|
| 意図分類 | **コード** | **AI** |
| ツール選定 | **コード** | **AI** |
| 引数生成 | **コード** | **AI** |
| MCP実行 | **コード** | **コード** |
| 結果の要約 | **AI**（日本語にする） | **AI**（日本語にする） |
| 精度 | ✅ ブレない | ⚠️ ブレるかも |

> **本命は定型対応。** 定型パターンを充実させればさせるほど、システムの信頼性が上がる。
> オープン対応は「おまけ」。あると便利だが、これに依存してはいけない。

### 2-13. テナント解決

> **MF MCPに`select_tenant`ツールは存在しない。**
> MCPは「1つのOAuthトークン = 1つの事業所」で固定。
> テナント切替はMCPではなく、sugu-sru側の責務。

```
ユーザー: 「株式会社すぐするの4月試算表を出して」

sugu-sru:
  ① 「株式会社すぐする」→ clients.json（将来Supabase）検索
  ② → clientId: c_XXXXXXXX を特定
  ③ → mfAuthService.getValidAccessToken(clientId) でOAuthトークン取得
  ④ → そのトークンでMCPクライアント接続
  ⑤ → 以降は通常のMCPツール呼び出し

コンテキストからの自動解決:
  - 顧問先詳細ページにいる → route.params.clientId から自動解決
  - チャット内で「この顧問先」→ 直前のコンテキストから解決
  - 曖昧な場合 → 「どの顧問先ですか？」と聞き返す
```

### 2-14. 英日辞書（`mfc_ca_en_ja_dictionary`）の活用

MF MCPには全ツールの説明に `Use 'mfc_ca_en_ja_dictionary' tool if you need Japanese translation.` と記載されている。
**MFはAIが最初にこの辞書ツールを呼ぶことを想定している。**

| 項目 | 仕様 |
|---|---|
| 呼び出しタイミング | テナント初回接続時に1回。結果をキャッシュ |
| キャッシュ有効期間 | 1日（テナントごと） |
| 用途 | APIレスポンスの英語キー → 日本語変換。AIの要約精度向上 |
| 定型対応での使い方 | コード側で辞書を参照し、AIに渡す結果JSONのキーを日本語に変換 |
| オープン対応での使い方 | AIのシステムプロンプトに辞書テキストを含めて理解を助ける |

---

## 3. WRITE確認モーダル — 設計（汎用）

> **仕訳だけではない。** 取引先作成、明細登録、データ更新など全WRITE操作に適用する汎用設計。

### 決定: 案C（チャット内インライン）ベース + 複合データは案B（テーブル）に自動切替

#### パターン1: 単純操作（取引先作成等）

```
AI: 取引先を新規登録します。よろしいですか？

  🏢 取引先情報
  ├── 名称: 山田商事株式会社
  ├── 法人番号: 1234567890123
  └── インボイス番号: T1234567890123

  [✅ 登録する]  [✏️ 修正する]  [❌ やめる]
```

#### パターン2: 仕訳作成（単純仕訳）

```
AI: 仕訳を作成します。よろしいですか？

  📋 テスト法人 — 2026/05/20
  ├── 借方: 消耗品費 ¥1,500（課税仕入10%）
  └── 貸方: 現金 ¥1,500
  摘要: 文具購入

  [✅ 作成する]  [✏️ 修正する]  [❌ やめる]
```

#### パターン3: 複合仕訳・複数行（テーブル自動切替）

```
AI: 複合仕訳を作成します。よろしいですか？

  📋 テスト法人 — 2026/05/20

  │ 借方          │ 金額    │ 貸方     │ 金額    │ 摘要       │
  │ 消耗品費      │ ¥1,500  │ 現金     │ ¥3,000  │ 文具・備品 │
  │ 事務用品費    │ ¥1,500  │          │         │            │

  ▼ 詳細を表示（API送信パラメータ）

  [✅ 作成する]  [✏️ 修正する]  [❌ やめる]
```

#### パターン4: データ更新（変更前後の差分表示）

```
AI: 仕訳を更新します。よろしいですか？

  📋 仕訳 #42 — テスト法人

  │ 項目     │ 変更前        │ 変更後        │
  │ 借方科目 │ 消耗品費      │ 事務用品費    │
  │ 金額     │ ¥1,500        │ ¥2,000        │

  ⚠️ この操作は取り消せます（操作ログから修復可能）

  [✅ 更新する]  [✏️ 修正する]  [❌ やめる]
```

#### パターン5: 一括操作（複数件同時）

```
AI: 5件の仕訳を一括作成します。よろしいですか？

  │ # │ 日付       │ 借方     │ 貸方 │ 金額    │
  │ 1 │ 2026/05/01 │ 消耗品費 │ 現金 │ ¥1,500  │
  │ 2 │ 2026/05/03 │ 交際費   │ 現金 │ ¥5,000  │
  │ 3 │ 2026/05/10 │ 旅費交通 │ 現金 │ ¥1,200  │
  │ ...残り2件 |

  [全件表示]  [✅ 5件すべて作成]  [1件ずつ確認]  [❌ やめる]
```

### 自動切替ルール

| 条件 | 表示パターン |
|---|---|
| フィールド3個以下（取引先作成等） | パターン1（ツリー表示） |
| 仕訳1行（単純仕訳） | パターン2（ツリー表示） |
| 仕訳2行以上（複合仕訳） | パターン3（テーブル表示） |
| 更新操作（既存データ変更） | パターン4（差分表示） |
| 複数件一括操作 | パターン5（一覧テーブル） |

---

## 4. UI構成

### Phase 1（sugu-sru内）

| UI要素 | 配置 | Chrome拡張との対応 |
|---|---|---|
| **フローティングAIボタン** | 画面右下 | → Side Panel に移植 |
| **テーブル行の右クリック「AIに聞く」** | 顧問先一覧・仕訳一覧等 | → Chrome context_menus に移植 |
| **Ctrl+L** | フローティングボタンの展開ショートカット | → chrome.commands に移植 |

### Phase 2（Chrome拡張）

| UI要素 | 配置 |
|---|---|
| Side Panel | Chatwork画面右側 |
| 右クリックメニュー | Chatwork上のどこでも |

---

## 5. AIの使い方の具体例（仕訳以外）

> 参考: [「仕訳を作る」だけじゃもったいない — マネフォMCPサーバーのもう一つの活用法](https://note.com/kyon_copanda/n/ne1a9a90ccfb8)
> 参考: [マネーフォワード クラウド × MCP連携 — AIエージェント実装パターン](https://aurant-technologies.com/blog/mcp-integration-cloud-15443/)

### 5-1. 経営者向け即答

```
ユーザー: 「今月の販管費はいくら？」
AI: テスト法人の2026年5月の販管費を集計します...

  📊 販管費合計: ¥1,234,567
  ├── 人件費: ¥800,000
  ├── 消耗品費: ¥150,000
  ├── 交際費: ¥80,000
  └── その他: ¥204,567

  前月比: +12.3%（4月: ¥1,098,765）

  [詳細内訳を見る]  [CSV出力]
```

### 5-2. 画面コンテキスト活用

```
ユーザー:（顧問先一覧ページで）「この一覧でMF未連携の顧問先は？」
AI: 表示中の14件の顧問先のうち、MF未連携は3件です。

  │ 顧問先名   │ MF連携状態 │
  │ ○○商事    │ ❌ 未連携   │
  │ △△工業    │ ❌ 未連携   │
  │ □□サービス │ ❌ 未連携   │

  [MF連携を開始]  [一覧をCSV出力]
```

### 5-3. テキスト選択 → 質問

```
ユーザー:（仕訳一覧で「事業主借」を選択 → 右クリック「AIに聞く」）
AI: 「事業主借」は個人事業で使う勘定科目です。
  個人のお金を事業に入れた場合に使います。

  テスト個人の事業主借の残高: ¥350,000（今期）

  [事業主借の仕訳一覧を見る]  [事業主貸との差額を確認]
```

### 5-4. データ整合チェック（sugu-sru ↔ MF）

```
ユーザー: 「MFとデータ整合チェックしろ」
AI: 全顧問先のMFデータとsugu-sruの整合性をチェックします...

  ✅ テスト法人: 決算月一致（6月）、科目数一致
  ⚠️ テスト個人: 決算月不一致
     sugu-sru: 11月 → MF: 12月
     [修正する]
  ⚠️ テスト個人: 仕訳件数差異
     sugu-sru: 42件 → MF: 45件（3件多い）
     [差分を確認する]

  [全詳細レポート]  [不一致のみ表示]
```

### 5-5. 複数システム横断

```
ユーザー: 「○○商事の全情報まとめて」
AI: ○○商事の情報を各システムから取得しました。

  🏢 基本情報（sugu-sru）
  ├── 担当: 山田太郎
  ├── 契約: Premium
  └── 進捗: 月次完了済み

  💰 会計情報（MF）
  ├── 決算月: 3月
  ├── 課税方式: 一般課税
  └── 今期仕訳: 234件

  [仕訳一覧を見る]  [進捗を更新]  [返信案を作成]
```

---

## 6. WAF 403 に関する追加情報

> **2026年5月1日**: マネーフォワードのGitHubへの不正アクセスが確認。
> ソースコードが閲覧された可能性があり、金融機関とのAPI連携が一時停止。
> 5月11日時点で銀行API連携再開に向けた進捗が案内されている。
>
> このセキュリティ対応として**Cloudflare WAFルールが一時的に厳格化**されている可能性が高く、
> 収束とともに緩和される見込みがある。
>
> **対処**: マネーフォワードのサポートニュースを定期確認。
> 開発者サポートへの直接問い合わせも有効（WAF 403はMF側インフラ設定のため自力突破不可）。

---

## 6. 関連ドキュメント

| ドキュメント | 関連 |
|---|---|
| [34_mf_mcp_integration.md](34_mf_mcp_integration.md) | MF MCP連携基盤（全19ツール） |
| [load_context.md](../../.agent/workflows/load_context.md) | API設計方針 |
| [28_api_migration_plan.md](28_api_migration_plan.md) | API化計画 |
