# SAFE MOCK DEVELOPMENT RULES

**Version**: 1.0  
**Project**: receipt-app  
**Status**: Mandatory  
**Scope**: src 配下全ファイル  
**制定日**: 2026-02-19  
**経緯**: ChatGPT・Claude・Google Antigravity 3者の議論を統合

---

## 0. 原則

- `any` は原則禁止
- 型エラーの無視は禁止
- 楽観的実装は禁止
- UIと型の乖離は禁止
- 「たぶん大丈夫」は禁止

> 型エラーは「消すもの」ではない。構造を修正するシグナルである。

---

## 1. フェーズ定義

### 🔧 Phase A-0（準備）

**目的**: 物理的制約を先に敷く  
**UIはまだ触らない。**

**実行者**: Google Antigravity（人間が1タスクずつ確認）

**手順**:
1. `Yen` 型の確定確認（確認のみ）
2. `journal_phase5_mock.type.ts` 確認（確認のみ）
3. `journal_test_fixture_30cases.ts` 確認（確認のみ）
4. `journalColumns.ts` 作成 → 人間確認 → commit
5. ヘッダーを `v-for` 化 → ブラウザ確認（人間） → commit
6. ボディを `v-for` 化 → ブラウザ確認（人間） → commit

> `mocks/unsafe/` 作成と ESLint ルール追加は Phase A 列実装が進んでから実施する。

**各タスク間で必ずブラウザ確認。一括置換は絶対禁止。**

> ⚠️ Phase A-0は安全策を講じて作業する。
> 方法A: `git branch columns-migration` で作業（失敗したらブランチを捨てる）
> 方法B: mainで作業前スナップショットコミットを打つ（失敗したら `git reset --hard` で復帰）

### 🔵 Phase A（UX探索モード）

**目的**: 触る・試す・探索する

#### 許可
- fixture直書き（既存30件は原則凍結）
- `mocks/unsafe/` 配下のみ `any` 許可（実験場）
- UI自由に触る（ただし columns.ts 経由必須）

#### 禁止
- Domain層（`src/shared/`, `src/types/`）の変更
- `Yen` 型の削除
- `unsafe/` から `data/` `components/` へのimport
- `mocks/data/` と `mocks/components/` での `any` 使用
- Supabase型の変更

#### 必須
- `journalColumns.ts` 経由でヘッダー・ボディ定義
- テーブル変更時のブラウザスクリーンショット
- width一致確認

#### 既存30件fixtureの扱い
- **原則**: データ値を変更しない
- **例外**: 新機能のUX検証に必要な場合のみ変更可
  - 例: 新ラベル追加（`NEED_CONSULT`等）、`memo` 追加、`export_exclude` 追加
  - 条件: 1回の作業で1-2件のみ変更
  - **変更前の値をコメントで残す**
  - 禁止: 全件一括変更
- **新規データ追加**: 31件目以降として追加（Phase BまではFactory不要、直書き可）

> **ルール3-4の適用時期**: Domain型強制（ガイド ルール3）と生成関数化（ガイド ルール4）は
> Phase B（スキーマ確定後）から適用する。Phase Aでは変更速度を優先し、fixture直書きを許容する。

### 🟡 Phase B（構造固定モード）

**移行条件**（全て満たすこと）:
- 全23列がブラウザで正しく表示（列数一致）
- ソート全項目動作確認済み
- モーダル全種動作確認済み
- トグル動作確認済み
- `npm run type-check` 通過
- `npm run lint` 通過
- `any` 増加ゼロ
- **ユーザーが「UX固定」と宣言**

#### Phase Bの内容
- 新規モックは Factory 必須
- CellComponent 分離
- fixture完全凍結

### 🔴 Phase C（Backend接続前）

- `any` 完全禁止
- fixture廃止
- Supabase Mapper 導入
- `unsafe/` ディレクトリ廃止
- ESLint strict 最大化

---

## 2. ディレクトリ安全構造

```
src/mocks/
  unsafe/        ← any許可（隔離・import禁止）
  data/          ← Domain型必須（any禁止）
  components/    ← Domain型必須（any禁止）
  types/         ← 聖域（any絶対禁止）
```

### ESLint ルール（物理的制約）
- `unsafe/` から `data/` `components/` へのimport → **error**
- `unsafe/` 以外で `any` 使用 → **error**
- `as any` → **error**
- `@ts-ignore` → 理由コメント+TODO必須

---

## 3. UIテーブル安全ルール（columns単一ソース化）

### 3.1 列定義は単一ファイル

```typescript
// src/mocks/columns/journalColumns.ts（2026-02-23更新）
export const journalColumns: JournalColumn[] = [
  // --- 操作 ---
  { key: "select",      label: "選",      width: "w-6",   type: "checkbox" },
  { key: "no",          label: "No.",     width: "w-8",   type: "index",     sortKey: "display_order" },
  // --- 特殊描画（component型） ---
  { key: "photo",       label: "写真",    width: "w-12",  type: "component", sortKey: "has_photo" },
  { key: "pastJournal", label: "過去仕訳", width: "w-14",  type: "component", sortKey: "past_journal" },
  { key: "rule",        label: "学習",    width: "w-8",   type: "component", sortKey: "rule" },
  { key: "comment",     label: "コメント", width: "w-12",  type: "component", sortKey: "staff_notes" },
  { key: "needAction",  label: "要対応",   width: "w-20",  type: "component", sortKey: "requires_action" },
  { key: "labelType",   label: "証票",    width: "w-12",  type: "component", sortKey: "label_type" },
  { key: "warning",     label: "警告",    width: "w-12",  type: "component", sortKey: "warning" },
  { key: "creditCardPayment", label: "クレ払い", width: "w-10", type: "component", sortKey: "is_credit_card_payment" },
  { key: "taxRate",     label: "軽減",    width: "w-8",   type: "component", sortKey: "tax_rate" },
  { key: "memo",        label: "証票メモ", width: "w-8",   type: "component", sortKey: "memo" },
  { key: "invoice",     label: "適格",    width: "w-10",  type: "component", sortKey: "invoice" },
  // --- データ（journal-level: text型） ---
  { key: "transaction_date",    label: "取引日",      width: "w-16",  type: "text",   sortKey: "transaction_date" },
  { key: "description",          label: "摘要",        width: "flex-1", type: "text",   sortKey: "description" },
  // --- データ（entry-level: text/amount型） ---
  { key: "debit.account",        label: "借方勘定科目", width: "w-20",  type: "text",   sortKey: "debit_account" },
  { key: "debit.sub_account",    label: "借方補助",    width: "w-16",  type: "text",   sortKey: "debit_sub_account" },
  { key: "debit.tax_category",   label: "借方税区分", width: "w-20",  type: "text",   sortKey: "debit_tax" },
  { key: "debit.amount",         label: "借方金額",    width: "w-16",  type: "amount", sortKey: "debit_amount" },
  { key: "credit.account",       label: "貸方勘定科目", width: "w-20",  type: "text",   sortKey: "credit_account" },
  { key: "credit.sub_account",   label: "貸方補助",    width: "w-16",  type: "text",   sortKey: "credit_sub_account" },
  { key: "credit.tax_category",  label: "貸方税区分", width: "w-20",  type: "text",   sortKey: "credit_tax" },
  { key: "credit.amount",        label: "貸方金額",    width: "w-16",  type: "amount", sortKey: "credit_amount" },
  // --- 操作（末尾） ---
  { key: "actions",     label: "操作",    width: "w-8",   type: "action",  icon: "⋮" },
];
```

### 3.2 ヘッダー直書き禁止

```vue
<!-- ❌ 禁止 -->
<div class="w-12">写真</div>
<div class="w-12">要対応</div>

<!-- ✅ 必須 -->
<div v-for="col in journalColumns" :key="col.key" :class="col.width">
  {{ col.label }}
</div>
```

### 3.3 ボディも同じcolumns参照

widthの一致が**物理的に保証**される。

### 3.4 列タイプ別の実装パターン

#### Phase A-0: 実装済みパターン（getValue + type分岐）

```typescript
// getValue: データパスkey（debit.account等）に対応するユーティリティ
function getValue(obj: any, path: string): unknown {
  return path.split('.').reduce((o: any, k: string) => o?.[k], obj)
}
```

```vue
<template v-for="col in journalColumns" :key="col.key">
  <!-- text型: journal-level（keyにドットなし）/ entry-level（keyにドットあり） -->
  <template v-if="col.type === 'text'">
    <div v-if="!col.key.includes('.') && rowIndex === 0" :class="col.width">
      {{ getValue(journal, col.key) }}
    </div>
    <div v-else-if="col.key.includes('.')" :class="col.width">
      {{ getValue(row, col.key) }}
    </div>
    <div v-else :class="col.width"></div>
  </template>

  <!-- amount型: undefined/null対策付き -->
  <template v-else-if="col.type === 'amount'">
    <div :class="col.width">
      {{ getValue(row, col.key) != null ? Number(getValue(row, col.key)).toLocaleString() : '' }}
    </div>
  </template>

  <!-- component型: col.key個別分岐（10列） -->
  <template v-else-if="col.type === 'component'">
    <template v-if="col.key === 'photo'">
      <i class="fa-solid fa-camera" ...></i>
    </template>
    <template v-else-if="col.key === 'needAction'">
      <!-- 4FAアイコン: NEED_DOCUMENT / NEED_INFO / REMINDER / NEED_CONSULT -->
      <div class="flex gap-0.5">
        <button v-for="noteKey in staffNoteKeys" :key="noteKey"
                @click="toggleStaffNote(journal.id, noteKey)" ...>
          <i :class="staffNoteConfig[noteKey].icon"></i>
        </button>
      </div>
    </template>
    <!-- 他のcomponent型列も同様にcol.key個別分岐 -->
  </template>
</template>
```

**Phase A-0の成果**:
- 全列のwidthが `journalColumns.ts` に集約される
- 物理的にwidth不一致が防げる
- `getValue()`でデータパスkey（`debit.account`等）に対応
- `key.includes('.')`でjournal-level/entry-level判定（Vue側ロジック）

#### Phase B: getCellComponent方式（綺麗に整理）

```typescript
import type { Component } from 'vue'

const cellComponents: Record<string, Component> = {
  photo: PhotoCell,
  pastJournal: PastJournalCell,
  comment: CommentCell,
  needAction: NeedActionCell,
  labelType: LabelTypeCell,
  warning: WarningCell,
  rule: RuleCell,
  taxRate: TaxRateCell,
  memo: MemoCell,
  invoice: InvoiceCell,
  // 列追加時はここに1行追加するだけ
}

function getCellComponent(key: string): Component | undefined {
  return cellComponents[key]
}
```

```vue
<template v-for="col in journalColumns" :key="col.key">
  <!-- text/amount型: getValue()経由 -->
  <template v-if="col.type === 'text'">
    <div :class="col.width">{{ getValue(row, col.key) }}</div>
  </template>
  <template v-else-if="col.type === 'amount'">
    <div :class="col.width">{{ getValue(row, col.key)?.toLocaleString() }}</div>
  </template>

  <!-- component型: 動的コンポーネント -->
  <template v-else-if="col.type === 'component'">
    <component :is="getCellComponent(col.key)" :journal="row" />
  </template>
</template>
```

**Phase Bでの整理**:
- v-if連鎖を解消
- セルコンポーネントに分離
- 列追加時は `cellComponents` に1行追加するだけ
- テンプレートは一切変更不要

### 3.5 列定義設計原則

- columns.tsは構造定義のみ。描画ロジックはVue側。
- keyは原則データパス（例: `transaction_date`, `debit.sub_account`）。component/checkbox/index/action型はUI識別子。
- 配列順 = UI表示順。
- type: Phase Aでは6種（checkbox/index/component/text/amount/action）から追加しない。
- sortKeyのみcolumns.tsで定義。ソート処理はVueコンポーネント側。

---

## 4. 型安全ルール

### 4.1 Domain型を必ず通す

```typescript
// ❌ 禁止
const mock = { amount: 1000 }

// ✅ 必須
const mock: JournalPhase5Mock = createMockJournal()
```

### 4.2 金額は Yen 型のみ

```typescript
// ❌ 禁止
amount: number

// ✅ 必須
amount: Yen
```

### 4.3 `unknown` を使う

```typescript
// ❌ 禁止
catch (err: any)

// ✅ 必須
catch (err: unknown)
```

---

## 5. LLM作業チェックリスト（必須）

### 作業前チェック
- [ ] Domain型は存在するか？
- [ ] columns定義は単一ソースか？
- [ ] 変更対象ファイルのgit statusを確認したか？

### 作業後チェック（型）
- [ ] `any` を増やしていない
- [ ] `as any` を使っていない
- [ ] `@ts-ignore` を使っていない
- [ ] 型キャストで逃げていない
- [ ] `Yen` 型を守っている

### 作業後チェック（UI）
- [ ] 列は `journalColumns.ts` のみ変更
- [ ] ヘッダーとボディの列数一致
- [ ] width一致
- [ ] ブラウザで変更前後を確認

### 作業後チェック（ビルド）
- [ ] `npm run type-check` 成功
- [ ] `npm run lint` 成功
- [ ] `npm run build` 成功

---

## 6. 完了報告フォーマット（強制）

LLMは作業後に以下を必ず報告する：

```
## 完了報告

### 変更ファイル一覧
- file1.ts（変更内容）
- file2.vue（変更内容）

### 型エラー数
- 変更前: X個
- 変更後: Y個

### any使用数
- 変更前: X個
- 変更後: Y個

### 影響範囲
- 影響を受けるコンポーネント一覧

### 構造破壊確認
- ヘッダー/ボディ列数一致: ✅/❌
- width一致: ✅/❌
- ブラウザ確認: ✅/❌（スクリーンショット添付）
```

**このフォーマットがない報告は無効。**

---

## 7. モック → Backend移行フロー

1. Domain型確定
2. Factory モック作成
3. UI構築（Domain型のみ依存）
4. Supabase型生成
5. Mapper作成（DB → Domain）
6. モック差し替え

> **UIはDomain型のみ依存。Supabase型を直接UIに渡さない。**

---

## 8. 禁止事項

- 複数箇所を一括変更
- 型エラーを消すためのキャスト
- UIテンプレートを columns.ts を通さず書き換える
- 動作確認なしで「完了」と報告
- モック構造を勝手に変更
- 指示されていない範囲の「ついで」修正

---

## 9. プロジェクト哲学

> **安全性 > 速度**  
> **構造 > 楽観**  
> **検証 > 自信**  
> **物理的制約 > 心理的制約**

LLMは補助者であり、設計者ではない。  
最終責任は構造にある。

---

## 10. 毎回AIに渡すプロンプト

以下をセッション開始時にAIに入力する：

---

### 📋 セッション開始プロンプト

```
あなたはreceipt-appプロジェクトのモック開発を補助するAIです。
以下のルールを必ず守ること。

【絶対ルール】
1. 作業前に docs/genzai/00_safe_mock_development_rules.md を読み込め
2. 作業前に docs/genzai/04_mock/components/モック作成ガイド.md を読み込め
3. 作業前に docs/genzai/02_database_schema/journal/journal_v2_20260214.md を読み込め

【現在のフェーズ】
Phase A（UX探索モード）

【作業スコープ復唱】
今回の作業スコープを復唱せよ。
スコープ外のファイルに触れた場合、その作業は全てrevertする。

【禁止事項】
- any使用（mocks/unsafe/ 以外）
- UIテンプレートの直接編集（columns.ts経由必須）
- 指示範囲外の「ついで」修正
- 動作確認なしの完了報告
- 既存30件fixtureの一括変更

【作業後の必須報告】
以下を必ず報告すること：
- 変更ファイル一覧
- 型エラー数（変更前 → 変更後）
- any使用数（変更前 → 変更後）
- ヘッダー/ボディ列数一致確認
- width一致確認
- ブラウザスクリーンショット

この報告がない場合は完了として認めない。
```

---

### 📋 テーブルUI変更時の追加プロンプト

```
テーブルUIの変更を行います。以下を追加で守ること。

1. 変更前にjournalColumns.tsの現在の列定義を全て表示せよ
2. 変更後にjournalColumns.tsの列定義を全て表示せよ
3. ヘッダーのwidthを全列リストアップせよ
4. ボディのwidthを全列リストアップせよ
5. 不一致があれば報告せよ
6. ブラウザスクリーンショットを撮影して添付せよ
7. git diff を出力せよ

この7つ全てを完了するまで「完了」と報告するな。
```

---

### 📋 新ラベル追加時のプロンプト

```
新しいラベルを追加します。以下を守ること。

1. journal_phase5_mock.type.ts の JournalLabelPhase5 に型が存在するか確認
2. 存在しなければ型定義に追加（types/は聖域、慎重に）
3. 既存fixtureへの追加は最大2件まで
4. 変更前の値をコメントで残すこと
5. ブラウザで表示確認
6. git diff を出力
```

---

### 📋 モーダル/コンポーネント追加時のプロンプト

```
新しいモーダル/コンポーネントを追加します。以下を守ること。

1. 既存データで検証可能か確認（触らなくて済むならfixture変更不要）
2. データ不足の場合はFactory経由で31件目以降として追加
3. 既存fixtureの変更は最終手段（1-2件、変更前コメント必須）
4. コンポーネントのpropsはDomain型のみ使用
5. ブラウザで動作確認
6. git diff を出力
```

---

## 11. スコープ検証プロトコル（暴走防止）

### 原則

LLMの「例外判断」「ついで修正」「善意の追加作業」を物理的に検出する。

> **ルール文書** → 読んでも忘れる・無視できる  
> **git diff** → 変更履歴が物理的に残る・人間が検証可能・嘘がつけない

---

### 作業開始時（LLMが宣言）

**必須宣言フォーマット:**

```
【スコープ宣言】

変更対象ファイル:
- src/mocks/components/JournalListLevel3Mock.vue
- src/mocks/columns/journalColumns.ts

新規作成予定ファイル:
- （なければ「なし」）

触らないファイル:
- 上記以外の全ファイル

修正内容詳細:
- 関数名・行範囲・何をどう変えるか
- 例: copyJournal() L698-718 — 即実行→確認ダイアログ追加

想定している問題:
- 壊れる可能性がある箇所
- 副作用（他の関数・テンプレートへの影響）
- テンプレートと噛み合わない可能性

検証方法:
- vue-tsc / eslint
- ブラウザ手動確認の具体的項目

理由:
- UIモック4列目の実装
```

**この宣言がない作業開始は認めない。**

---

### 作業終了時（LLMが検証）

**必須実行コマンド:**

```bash
# 1. 変更ファイル一覧を表示
git diff --stat

# 2. スコープ宣言と照合
# スコープ外のファイルがあれば報告

# 3. スコープ外ファイルを元に戻す
git checkout <スコープ外のファイル>
```

**完了報告に以下を含めること:**
- `git diff --stat` の出力
- スコープ宣言との一致確認
- スコープ外変更があった場合の理由と対処

---

### 人間の検証

**人間は以下を確認する:**

1. `git diff --stat` を自分でも実行
2. スコープ宣言と照合
3. 不一致があれば作業全体を却下

**検証コマンド例:**

```bash
# 全体の変更確認
git diff --stat

# 特定ディレクトリの変更行数
git diff -- src/mocks/data/ | wc -l

# fixture変更が3件以上か確認
git diff -- src/mocks/data/journal_test_fixture_30cases.ts
```

---

### 各暴走パターンの検出

| 暴走パターン | 検証方法 | 判定基準 |
|---|---|---|
| ついで修正 | `git diff --stat` | スコープ外ファイル出現 = 違反 |
| ブラウザ未確認 | スクショファイル確認 | `browser_subagent` キャプチャなし = 違反 |
| fixture一括変更 | `git diff -- src/mocks/data/` | 3件以上変更 = 違反 |
| 勝手なバグ修正 | `git diff --stat` | スコープ外ファイル出現 = 違反 |

---

### 将来的な自動化（Phase B以降）

**pre-commit hook** でスコープ検証を自動化する:

```javascript
// .githooks/scope-check.js（Node.js）
const fs = require('fs');
const { execSync } = require('child_process');

// スコープ宣言ファイル読み込み
if (!fs.existsSync('.scope-declaration.txt')) {
  console.error('❌ エラー: スコープ宣言がありません');
  process.exit(1);
}

const declared = fs.readFileSync('.scope-declaration.txt', 'utf8')
  .split('\n').filter(Boolean);
const changed = execSync('git diff --cached --name-only')
  .toString().split('\n').filter(Boolean);

const undeclared = changed.filter(f => !declared.includes(f));

if (undeclared.length > 0) {
  console.error('❌ スコープ外ファイルが変更されています:');
  undeclared.forEach(f => console.error(`  - ${f}`));
  process.exit(1);
}

console.log('✅ スコープ検証: 合格');
```

**運用フロー:**
1. 作業開始時: LLMが `.scope-declaration.txt` 作成
2. 作業中: 自由に作業
3. commit時: pre-commit hookが自動検証 → スコープ外変更はcommit拒否

**限界:**
- `--no-verify` でhookを無視できる → **人間が `git diff --stat` で最終確認**
- `.scope-declaration.txt` を書き換えられる → **人間がdiffで検出**

> 完全ではないが、暴走の90%は検出できる。残り10%は人間の最終確認で補う。

---

## 12. 未検証の情報を人間に提示するな（推測提示禁止）

> **原則**: 出力に未検証の事実を含めるな。
> 不明な点があれば、説明する前にツールで確認しろ。
> 確認できるまで「確認する」とだけ書け。

- ❌ 推測を提示:「○○の可能性がある」
- ✅ 事実のみ提示:「○○と表示された。確認する。」→ ツールで確認 → 報告
